# 在线考试管理系统开发 — 课堂笔记

## 项目概述

基于 **Java Servlet + JSP + MySQL** 的在线考试管理系统，包含三种角色：管理员、教师、学生。采用三层架构：JSP（视图层）→ Servlet（控制层）→ DAO（数据访问层）。

---

## 一、多班级发布试卷的存储设计问题

### 遇到的问题
一张试卷需要同时发布到多个班级（如"计算机1班,计算机2班,大数据1班"），但数据库 `lwmexampaper.lwmclassname` 字段是单个 VARCHAR，无法存储多个班级。

### 解决方案
采用**逗号分隔存储**方案（方案 A）：
- 将多个班级名拼接为 `"计算机1班,计算机2班,大数据1班"` 存入单个字段
- 字段长度从 `VARCHAR(20)` 扩大到 `VARCHAR(200)`

### 关键技术点
| 场景 | 原来的SQL | 改后的SQL |
|------|----------|----------|
| 学生查询本班试卷 | `WHERE p.lwmclassname = ?` | `WHERE FIND_IN_SET(?, p.lwmclassname)` |
| 教师按班级筛选试卷 | `AND p.lwmclassname = ?` | `AND p.lwmclassname LIKE CONCAT('%', ?, '%')` |

**核心知识点**：MySQL 的 `FIND_IN_SET(str, strlist)` 函数用于在逗号分隔的字符串列表中精确查找元素，避免了 `LIKE '%1班%'` 可能匹配到 "1班" 和 "11班" 的歧义问题。

### 连带设计调整
- 创建试卷时不再选班级（`lwmclassname` 初始为空字符串）
- 新增独立的"发布试卷"页面，用 checkbox 多选班级
- 试卷编辑时禁止修改班级（发布后由专门的发布页管理）

---

## 二、考试时间访问控制问题

### 遇到的问题
教师设置了考试的开始时间和结束时间，但学生可以在任意时间进入考试——考试时间字段形同虚设。

### 解决方案
在 `lwmTakeExam.java`（考试入口 Servlet）中加入**服务端时间校验**：

```java
// 获取当前时间
Timestamp now = new Timestamp(System.currentTimeMillis());

if (now.before(startTime)) {
    // 阻止进入，提示"考试还未开始"
} else if (now.after(endTime)) {
    // 阻止进入，提示"考试已结束"
} else {
    // 允许进入考试
}
```

### 设计要点
- **开始前**：试卷可见但不能进入，显示"考试还未开始"
- **进行中**：可以进入考试
- **结束后**：已在考试中的学生**不受影响**，可以继续作答并提交；但**新学生不能再进入**
- 前端 JSP 同时显示状态标签（未开始/进行中/已结束），禁用"开始考试"按钮

### 学到的东西
时间控制必须在**服务端**做，不能只靠前端 JS。前端 JS 用户可以绕过，服务端才是真正的安全边界。

---

## 三、考试倒计时与强制交卷

### 遇到的问题
`lwmexamtime` 字段（考试时长，单位分钟）仅在页面上显示，没有任何实际约束力。学生可以无限时作答。

### 解决方案

#### 3.1 服务端记录开始时间
在 `lwmTakeExam.java` 中，学生**首次进入考试**时创建考试记录，将 `lwmstarttime` 设为当前时间：

```java
// 没有草稿记录 → 首次进入 → 记录开始时间
if (record == null) {
    record = new lwmExamRecord();
    record.setLwmstarttime(new Timestamp(System.currentTimeMillis()));
    dao.insert(record);
}
```

已有草稿记录时不重置开始时间，保证**刷新页面不会重新计时**。

#### 3.2 前端倒计时
```javascript
// 剩余秒数 = deadline - 当前时间
let deadline = recordStartTime + examTime * 60 * 1000;
let remaining = (deadline - Date.now()) / 1000;

// 显示格式 HH:MM:SS，不足5分钟变红色
// 归零时自动提交试卷
```

#### 3.3 服务端提交时二次校验
```java
long deadline = record.getLwmstarttime().getTime() + paper.getLwmexamtime() * 60 * 1000L;
if (System.currentTimeMillis() > deadline) {
    out.print("<script>alert('考试时间已到，无法提交');history.back();</script>");
    return;
}
```

### 学到的东西
- 前端倒计时用于**用户体验**，服务端校验用于**安全兜底**
- 首次进入记录时间 + 后续进入复用已有时间，解决了页面刷新重置计时器的隐患
- 倒计时归零自动提交时，`当前时间 ≈ deadline`，仍在服务端允许范围内

---

## 四、答题卡侧边栏设计

### 遇到的问题
考试页面问题较多时（50+题），学生需要来回滚动查看作答情况，不知道哪些题还没做。

### 解决方案
在考试页面左侧添加**答题卡侧边栏**，纯前端实现：

```
布局：210px侧边栏（sticky吸附） + flex主内容区
```

### 核心实现

1. **题号按钮网格**：按题型分组显示题号按钮，绿色=已答，灰色=未答
2. **点击跳转**：`data-qid` 对应 `question_<id>`，`scrollIntoView({behavior:'smooth'})` 平滑滚动
3. **实时变色**：委托监听题目的 `change`/`input` 事件，自动切换按钮颜色
4. **折叠功能**：侧边栏可收起至 40px 宽度，竖排显示"答题卡"
5. **草稿恢复**：页面加载时根据 `draftAnswers` 初始化按钮状态

### 学到的东西
- `position: sticky; top: 24px` 实现跟随滚动的吸附效果
- `scrollIntoView({behavior: 'smooth', block: 'center'})` 平滑滚动到指定元素
- 事件委托在父级监听，避免为每个题目单独绑定事件
- 纯前端功能，无需后端改动

---

## 五、未答题提醒

### 遇到的问题
学生提交试卷时可能漏掉题目，但系统没有任何提示。

### 解决方案
提交试卷时（手动点击提交按钮），遍历所有题目检查作答状态：

```javascript
function countUnanswered() {
    let count = 0;
    // 单选/判断：检查是否有 radio 被选中
    // 多选：检查是否有 checkbox 被选中
    // 简答：检查 textarea 是否非空
    return count;
}

if (unanswered > 0) {
    if (!confirm('还有 ' + unanswered + ' 道题未作答，确定要提交吗？')) {
        return;  // 用户取消提交
    }
}
```

### 设计注意
- 倒计时归零自动提交时**不弹提醒**（弹窗会阻塞自动提交）
- 允许交白卷（confirm 确认后仍然可以提交）

---

## 六、成绩分析中的及格线硬编码问题

### 遇到的问题
成绩分析页面中，及格线硬编码为 **60 分**。但不同试卷满分不同（有的 100 分，有的 150 分），60 分的及格线对 150 分试卷没有意义。

### 解决方案
从试卷表中读取 `lwmexamsore`（试卷总分），按**百分比**动态计算阈值：

```java
int totalScore = paper.getLwmexamsore();
int passLine  = (int)(totalScore * 0.6);   // 及格线 = 总分 × 60%
int excelLine = (int)(totalScore * 0.9);   // 优秀线 = 总分 × 90%
```

然后将 SQL 中的硬编码 `60` 和 `90` 全部替换为 Java 变量 `passLine` 和 `excelLine`。

### 连带改动
- 成绩分布图的分数区间标签改成动态的
- 学生等级标签（优秀/良好/中等/及格/不及格）改为基于百分比

### 学到的东西
- 硬编码是 bug 的温床——凡是"阈值"数字，都应该从数据源动态计算
- 百分比优于绝对值：60% 对所有试卷都适用

---

## 七、班级比较页面的三个 Bug

### Bug 1：班级下拉框显示"一班,二班"作为整体

**根因**：`lwmexampaper.lwmclassname` 存的是逗号分隔的多班级字符串，直接放入下拉框导致 `"计算机1班,大数据1班"` 被当作一个选项。

**解决**：加载班级列表后，对每个值按逗号 split，去重后放入 `Set<String>`，再传递给 JSP：

```java
Set<String> classSet = new LinkedHashSet<>();
for (String name : classNames) {
    for (String part : name.split(",")) {
        String trimmed = part.trim();
        if (!trimmed.isEmpty()) classSet.add(trimmed);
    }
}
```

### Bug 2：班级比较页科目切换后试卷下拉不刷新

**根因**：原实现用 `style.display='none'` 隐藏 `<option>`，这在各浏览器中行为不一致。

**解决**：页面加载时将所有 paper 选项缓存到 JS 数组，切换科目时**清空下拉框重建**选项列表：

```javascript
let allPaperOptions = [];  // 页面加载时缓存
function onSubjectChange() {
    paperSelect.innerHTML = '<option value="">请选择试卷</option>';
    allPaperOptions.forEach(opt => {
        if (opt.subjectId == selectedSubjectId) {
            // 重建匹配的 option
        }
    });
}
```

### Bug 3：第二次比较时图表不刷新

**根因**：`startCompare()` 使用了 `coreMetricsRendered` 等一次性标志，第一次比较后标志为 `true`，第二次直接 return。

**解决**：每次 `startCompare()` 开始时重置标志、销毁旧图表、清空旧 HTML：

```javascript
function startCompare() {
    coreMetricsRendered = false; distRendered = false; kpLoaded = false;
    coreChartInst && coreChartInst.dispose();   // 销毁旧ECharts实例
    distChartInst && distChartInst.dispose();
    document.getElementById('coreTable').innerHTML = '';
    // ...然后重新渲染
}
```

### 学到的东西
- 一次性标志位是常见的导致"第二次操作无效"的原因
- ECharts 实例需要 `dispose()` 销毁后再重建，否则可能内存泄漏
- 隐藏 `<option>` 不可靠，重建 DOM 才是可靠做法

---

## 八、防止取消发布有已提交记录的班级

### 遇到的问题
教师在发布管理中可以随意取消班级的发布，即使该班级已经有学生完成了考试。这会导致学生成绩与试卷脱钩。

### 解决方案

#### 前端
已提交的班级 checkbox **禁用**，并显示警告图标：

```java
// 后台查询哪些班级已有提交记录
Set<String> submittedClasses = dao.hasSubmitRecord(paperId);
// 传递到JSP，checkbox: disabled + 警告标记
```

同时加入**隐藏 input** 保证禁用的 checkbox 值仍被提交：
```html
<input type="hidden" name="submittedClasses" value="<%=cls%>">
```

#### 后端二次校验
```java
// 计算"被取消的班级" = 之前发布的 - 本次选择的
for (String removed : removedClasses) {
    if (hasSubmittedStudents(paperId, removed)) {
        out.println("<script>alert('班级 " + removed + " 已有学生完成考试，无法取消发布');</script>");
        return;
    }
}
```

### 学到的东西
- 前端 `disabled` 的控件不会随表单提交，需要配合隐藏域
- 安全校验必须是**前端 + 后端双层**，不能只靠前端禁用

---

## 九、输入验证：成绩、考试时间、试题分值

### 遇到的问题
三个输入验证缺失：
1. **评分**：可以输入负数得分
2. **考试时间**：可以设为 0 分钟
3. **每题分值**：可以输入负数

JSP 虽然有 `min="0"`，但用户可以绕过 HTML 属性直接发请求。

### 解决方案

#### 9.1 评分校验

**前端 JS**（在原有的 max 检查前加上 min 检查）：
```javascript
if (score < 0) {
    alert('第' + (i + 1) + '题得分不能为负数');
    return false;
}
```

**服务端**（将静默钳位改为明确拒绝）：
```java
// 改前：if (maxScore > 0 && score > maxScore) score = maxScore;  // 静默钳位，不报错
// 改后：
if (score < 0 || (maxScore > 0 && score > maxScore)) {
    out.println("<script>alert('分数无效，应在0到" + maxScore + "分之间');history.go(-1);</script>");
    return;
}
```

#### 9.2 考试时间校验
- JSP：`<input type="number" min="1" required>`（原来没有 min）
- Servlet：`if (examTime <= 0) { 提示"考试时间必须大于0分钟"; return; }`

#### 9.3 每题分值校验
- JSP 已有 `min="0"`，Servlet 新增：`if (danxScore < 0 || duoxScore < 0 || pdScore < 0 || jdScore < 0) { 提示"试题分值不能为负数"; return; }`

### 后续修复
发现只检查 `>= 0` 不够——每道题分值必须 `> 0`，因为设为 0 意味着题目白做：
```java
if (danxScore <= 0 || duoxScore <= 0 || pdScore <= 0 || jdScore <= 0) {
    // 必须大于0
}
```

### 学到的东西
- HTML `min`/`max` 属性只影响浏览器端的输入控件，不能作为安全边界
- **服务端验证是必须的**，永远不能信任客户端传来的数据
- 静默修正（clamping）不如明确报错——前者让用户不知道数据被改了

---

## 十、限制试卷只能发布到教师所教科目的班级

### 遇到的问题
发布试卷时，班级列表显示了该教师在**所有**科目下教的班级。比如教师教数学（1班、2班）和英语（仅1班），发布英语试卷时 2 班也出现了——但该教师不教 2 班的英语。

### 解决方案

#### 班级列表过滤（GET）
```sql
-- 改前
SELECT DISTINCT lwmclassname FROM lwmstudentcourseteacher WHERE lwmteacherid = ?

-- 改后（加上 subjectId 条件）
SELECT DISTINCT lwmclassname FROM lwmstudentcourseteacher
WHERE lwmteacherid = ? AND lwmsubjectid = ?
```

#### 后端校验（POST）
对每个选中的班级，验证教师-科目-班级的对应关系是否存在：

```java
for (String className : selectedClasses) {
    int count = dao.countByTeacherSubjectClass(teacherId, subjectId, className);
    if (count == 0) {
        invalidClasses.add(className);  // 不是该课程的授课班级，过滤掉
    }
}
```

如果全部班级都不合法：提示"所选班级均非本课程授课班级，发布失败"。

### 学到的东西
- 数据权限控制不应只靠前端隐藏，后端必须做**关联校验**
- 班级列表的查询条件必须和实际业务关系（排课表）对齐

---

## 十一、防止删除已被试卷引用的试题

### 遇到的问题
删除试题时没有安全检查。由于外键设置了 `ON DELETE CASCADE`，删除试题会级联删除 `lwmpaperquestion` 中的关联记录，导致已发布的试卷、甚至已有学生提交的试卷中丢失题目。

### 解决方案（仿照试卷删除的保护模式）

在 `lwmDeleteQuestion.doGet()` 中增加预删除检查：

```java
// 1. 查询哪些试卷引用了该试题
List<lwmExamPaper> papers = paperDAO.getPapersByQuestionId(questionId);

// 2. 如果有引用，阻止删除并列出试卷名
if (!papers.isEmpty()) {
    String msg = "该试题已被以下试卷引用，无法删除：\n";
    for (lwmExamPaper p : papers) {
        msg += "- " + p.getLwmpapername() + "\n";
    }
    msg += "\n请先从对应试卷中移除该试题后再删除。";
    out.println("<script>alert('" + msg + "');history.back();</script>");
    return;
}

// 3. 没有引用，正常删除
dao.lwmDeleteQuestion(id);
```

### DAO 新增方法
```java
public List<lwmExamPaper> getPapersByQuestionId(int questionId) {
    String sql = "SELECT p.* FROM lwmexampaper p "
               + "INNER JOIN lwmpaperquestion pq ON p.lwmpaperid = pq.lwmpaperid "
               + "WHERE pq.lwmquestionid = ?";
    // ...
}
```

### 学到的东西
- `ON DELETE CASCADE` 是双刃剑：方便清理但危险，写操作前要多做引用检查
- 参考已有模式（`lwmDeletePaper` 的 `hasSubmitRecord()` 检查），保持了代码风格一致性

---

## 十二、知识点删除的安全控制

### 遇到的问题
知识点管理功能缺少删除能力，但直接添加删除又可能影响已有试题的知识点关联。

### 解决方案

#### 前后端配合的安全删除流程

1. **前端**：每个知识点标签旁加红色 `×` 按钮，点击时 `confirm()` 确认
2. **异步请求**：`fetch` POST 到 servlet（`action=delete`）
3. **后端安全校验**：

```java
// 检查是否有试题引用了该知识点
int count = kpDAO.countQuestionsByKP(kpId);
if (count > 0) {
    return json({success: false, message: "该知识点已被试题使用，无法删除"});
}
// 无引用，可以删除
kpDAO.delete(kpId);
return json({success: true});
```

4. **前端刷新**：删除成功后从 `existingKpIds` 数组中移除该 ID，重新渲染知识点列表

#### 小细节修复
将前端请求参数用 `URLSearchParams` 构建，添加 `deleteKPInProgress` 标志防止重复点击。

### 学到的东西
- AJAX 删除比表单提交体验更好（无页面刷新）
- 防止重复点击（loading guard flag）是常见的 UX 优化
- JSON 响应 + 前端判断成/败的处理模式

---

## 十三、学生端单页面改造为 frameset 布局

### 遇到的问题
学生端的 `lwmstudent_main.jsp` 把考试中心、我的试卷、个人信息三个模块全部塞在一个 JSP 中，代码长达 800 行，维护困难，导航切换靠 JS 标签页控制。

### 解决方案
参照教师端的 frameset 布局进行改造：

```
lwmstudent_main.jsp (frameset)
├── topFrame    → lwmstudent_top.jsp    （顶栏）
├── leftFrame   → lwmstudent_left.jsp   （侧栏导航）
└── rightFrame  → lwmstudent_index.jsp  （考试中心，默认页）
                  lwmstudent_paper.jsp  （我的试卷）
                  lwmstudent_message.jsp（个人信息）
```

### 改动要点
- 每个模块拆成独立 JSP，自带 `<style>` 块
- 导航用 `<a target="rightFrame">` 切换内容
- 移除 `module-panel`、`data-module`、标签切换 JS 等不再需要的代码
- 保持学生端橙/金色主题

### 学到的东西
- 页面拆分原则：一个 JSP 只负责一个功能模块
- frameset 的 `target` 属性控制链接在哪一帧打开
- 当相似的布局（教师端 frameset）已存在时，遵循现有模式是最好的实践

---

## 十四、frameset 嵌套问题

### 遇到的问题
学生提交考试后，返回页面嵌套到了 `rightFrame` 内部，形成了 frame 套 frame 的嵌套结构。

### 解决方案
将返回导航从 `location.href` 改为 `parent.location.href`：

```javascript
// 错误：在当前 frame 内跳转
location.href = 'lwmstudent_index.jsp';

// 正确：在顶级窗口跳转，打破 frameset 嵌套
parent.location.href = 'lwmstudent_index.jsp';
```

### 学到的东西
- frameset 环境下，`location` 作用于当前 frame，`parent.location` 作用于父窗口
- "嵌套 frame" 是 frameset 架构的常见 bug，用 `parent.location.href` 或 `target="_parent"` 解决

---

## 十五、总结：贯穿始终的开发原则

| 原则 | 具体体现 |
|------|---------|
| **服务端验证是安全底线** | 考试时间校验、分数范围校验、发布班级校验，全都在服务端做二次校验 |
| **硬编码是 bug 的温床** | 60分及格线改为动态百分比计算 |
| **参考已有模式** | 试题删除保护照着试卷删除保护写；学生端布局照着教师端写 |
| **先查后删** | 删除试题前检查是否被试卷引用，取消发布前检查是否有提交记录 |
| **前后端配合** | 前端禁用 + 服务端拒绝，双层防护 |
| **一次性标志要重置** | 班级比较页第二次不可用的根本原因 |
| **DOM 操作而非 CSS 隐藏** | 科目切换重建 `<option>`，而非 `display:none` |

---

## 十六、数据库设计知识点

### 逗号分隔存储的适用场景
- **适用**：简单的列表存储，无需独立查询列中的单个值
- **不适用**：需要 JOIN、需要独立索引、需要高并发更新的场景
- **关键函数**：`FIND_IN_SET(str, list)` 精确匹配

### 级联删除的风险
- `ON DELETE CASCADE` 方便但不安全
- 业务上有引用关系的记录，删除前应做引用检查

### 多对多关系
- 试题 ↔ 知识点：通过 `lwmquestionknowledge` 中间表
- 试卷 ↔ 试题：通过 `lwmpaperquestion` 中间表
- 用 `UNIQUE` 约束防止重复关联
