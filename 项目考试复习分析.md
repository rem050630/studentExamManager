# 在线考试系统（lwmexam）期末复习分析

---

## 一、项目概述

本项目是一个**基于 Java Web 的在线考试管理系统**，采用 Servlet + JSP 技术架构，MySQL 作为数据库，Maven 进行项目构建管理。

### 技术栈
| 层级 | 技术 |
|------|------|
| 前端 | JSP + HTML + CSS + JavaScript |
| 控制层 | HttpServlet（原生 Servlet） |
| 数据访问层 | DAO 模式 + JDBC 直接连接 |
| 数据库 | MySQL 8.0 |
| 构建工具 | Maven（war 包） |
| 第三方库 | JExcelApi（Excel 导入导出） |

---

## 二、系统架构分析

### 2.1 三层架构（非标准 MVC，但接近）

```
JSP(视图层) → Servlet/Action(控制层) → DAO(数据访问层) → MySQL
```

- **Action 层**（`action/lwmexam/`）：所有 Servlet 类，每个都标注 `@WebServlet` 注解，处理 HTTP 请求
- **Service 层**（`service/lwmexam/`）：仅 3 个工具类：
  - `MysqlConn.java`：数据库连接、查询、更新
  - `Fpage.java`：分页工具
  - `ExcelBook.java`：Excel 导入导出
- **DAO 层**（`dao/lwmexam/`）：数据访问对象，每个表对应一个 DAO
- **Entity 层**（`entity/lwmexam/`）：POJO 实体类，与数据表一一对应

### 2.2 数据库设计（11张表）

| 表名 | 用途 | 核心字段 |
|------|------|----------|
| `lwmadmin` | 管理员信息 | 账号、密码、姓名 |
| `lwmstudent` | 学生信息 | 学号、姓名、密码、性别、年级、专业、班级 |
| `lwmteacher` | 教师信息 | 工号、姓名、密码、性别、电话 |
| `lwmexamsubject` | 考试科目 | 科目名、代码、学分、学期 |
| `lwmexamquestion` | 试题库 | 科目ID、题型、内容、选项ABCD、正确答案 |
| `lwmexampaper` | 试卷信息 | 名称、科目、考试时间、开始/结束时间、各类题型数量和分值、班级、总分 |
| `lwmpaperquestion` | 试卷-试题关联 | 试卷ID、试题ID（多对多中间表） |
| `lwmexamrecord` | 考试记录 | 试卷ID、学生ID、开始时间、结束时间、提交状态 |
| `lwmstudentanswer` | 学生答题详情 | 记录ID、试题ID、学生答案、得分 |
| `lwmexamscore` | 成绩表 | 记录ID、总分、阅卷教师ID、评分时间 |
| `lwmstudentcourseteacher` | 选课-教师关联 | 班级、科目、教师、学期（排课表） |
| `lwmknowledgepoint` | 知识点 | 科目ID、知识点名称、描述 |
| `lwmquestionknowledge` | 试题-知识点关联 | 试题ID、知识点ID |
| `lwmmistakebook` | 错题本 | 学生ID、试题ID、是否做错、复习状态 |

---

## 三、核心代码逐个分析

### 3.1 Service 层

#### MysqlConn.java —— 数据库连接工具
```java
// 关键：JDBC 四大要素
String Driver = "com.mysql.cj.jdbc.Driver";  // MySQL 8.x 驱动
String url = "jdbc:mysql://localhost:3306/lwmexam?...";
String user = "root";
String password = "123456";
```
- `getConnection()`：加载驱动 + 建立连接（Class.forName + DriverManager.getConnection）
- `doQuery(sql, param[])`：执行查询，返回 ResultSet，使用 **PreparedStatement** 防止 SQL 注入
- `doUpdate(sql, param[])`：执行增删改，返回影响行数
- `close()`：关闭 rs → pstmt → conn（顺序必须正确）

#### Fpage.java —— 分页工具
- `pageSize = 6`：每页6条
- `rowCount` 设置时自动计算 `pageCount`：若能整除 `rowCount/pageSize`，否则 `+1`
- `start = pageNow * pageSize`：MySQL LIMIT 起始位置
- `setFpage(sql, param)`：执行 COUNT 查询获取总记录数

#### ExcelBook.java —— Excel 工具类
- `excelOut(path, rs, params)`：将 ResultSet 导出为 .xls 文件，使用 JExcelApi 的 WritableWorkbook
- `ExcelIn(path)`：从 .xls 文件读取数据，返回 `ArrayList<String[]>`

### 3.2 Action 层 —— 核心业务 Servlet

#### lwmLogin.java —— 三合一登录
- URL：`@WebServlet("/lwmLogin")`
- 根据 `role` 参数区分三种登录：admin / student / teacher
- 分别查询 `lwmadmin` / `lwmstudent` / `lwmteacher` 表
- 登录成功后将用户对象和 role 存入 **HttpSession**
- 登录失败用 `alert()` + `history.go(-1)` 返回

#### lwmTakeExam.java —— 学生进入考试
- URL：`@WebServlet("/lwmTakeExam")`
- **关键流程**：
  1. 校验学生是否登录（从 Session 获取）
  2. **班级匹配校验**：检查学生班级是否在试卷发布的班级列表中（逗号分隔匹配）
  3. **时间控制校验**：`now.isBefore(startTime)` → "考试还未开始"；`now.isAfter(endTime)` → "考试已结束"
  4. 加载试题并按题型排序（单选→多选→判断→简答）
  5. **草稿恢复**：查找 `lwmsubmitstatus=0` 的记录，恢复之前保存的答案
  6. 若无草稿则创建新考试记录，`starttime` 为当前时间
- 时间解析支持多种格式：`yyyy-MM-dd HH:mm:ss`、`yyyy-MM-dd'T'HH:mm`等

#### lwmSubmitExam.java —— 学生提交考试
- **防超时提交**：`recordStartTime + examTime(分钟)` 与当前时间比较，超出则拒绝
- `autoSubmit` 参数标记自动交卷时容忍 5 秒误差
- 更新 `lwmsubmitstatus=1`（已提交）
- **先删后插**：删除旧答案，重新保存所有答案
- 未作答的题目也插入空字符串

#### lwmSaveExamDraft.java —— 保存草稿
- 逻辑与提交类似，但只插入答案，不改变 `lwmsubmitstatus=0`
- 使用 `parent.location.href` 刷新父框架

#### lwmCreatePaper.java —— 创建试卷（核心业务）
- **两种组卷模式**：
  - `manual`：手动选题，前端传 questionIds[]
  - `random`：随机组卷，指定各题型数量，调用 `lwmRandomPick()` （MySQL `ORDER BY RAND() LIMIT N`）
- **输入验证**：
  - 考试时间 > 0
  - 各题型分值 > 0
  - 开始时间 < 结束时间
  - 随机组卷时检查题库数量是否满足需求
- **重名检查**：同一教师不能创建同科目+同名的试卷
- 总分自动计算：`各题型数量 × 各题型分值` 之和

#### lwmPublishPaper.java —— 试卷发布（最复杂的单项业务）
- **GET 请求**：加载发布页面
  - 从 `lwmstudentcourseteacher` 查该教师该科目的授课班级
  - 查已发布班级和已有提交记录的班级（不能取消发布）
- **POST 请求**：保存发布设置
  - **验证**：勾选的班级必须在 `lwmstudentcourseteacher` 中存在（教师必须教该班的该科目）
  - 不满足条件的班级自动跳过并提示
  - **反向验证**：取消发布某班级时，检查该班是否已有学生提交，如有则拒绝

#### lwmGradeExam.java —— 教师阅卷（进入批阅界面）
- 检查 `lwmsubmitstatus != 2`（成绩未最终提交时才能批阅）
- **自动评分**（仅首次，各题分数为0时触发）：
  - 单选/判断：`studentAns.equals(correctAns)` → 满分或0
  - 多选：标准化答案（支持 "A,C,D" 和 "ACD" 两种格式），排序后比较
  - 简答题：不自动评分，等待教师手动打分
- 从试卷配置加载各类题型分值

#### lwmSubmitScore.java —— 教师提交成绩
- **分数校验**：每道题分数必须在 0 到该题型最高分之间
- 保存每题得分后计算总分，插入 `lwmexamscore` 表
- **自动记录错题**：调用 `recordMistakes()`，得分 < 该题型满分 → 标记为错题
- `finalize=true` 时设置 `lwmsubmitstatus=2`，之后不可再修改

#### lwmMistakeBookAction.java —— 错题本
- 支持按科目、知识点、复习状态筛选
- 分页显示
- POST 请求更新复习状态：0=未复习 → 1=已复习 → 2=已掌握

#### lwmScoreAnalysisAction.java —— 成绩分析
- **统计分析指标**：
  - 平均分、最高分、最低分、标准差
  - 及格率（≥总分×60%）、优秀率（≥总分×90%）
  - 五段分布统计
- **班级对比模式**（`action=compare`）：多班级横向比较，返回 JSON
- 动态分数线：根据试卷实际总分计算及格线/优秀线

#### lwmKnowledgeAnalysisAction.java —— 知识点掌握分析
- 按试卷统计各知识点得分率
- 支持分班级对比
- 标记弱项知识点（得分率 < 60%）

#### lwmRandomPickQuestions.java —— 随机抽题 AJAX 接口
- 按题型和数量随机抽取，排除已选题目
- 返回 HTML 片段供前端动态插入

#### lwmViewExam.java —— 学生查看考试详情
- 仅允许查看已提交（`status != 0`）的试卷
- 显示每题得分、正确答案、总分

#### lwmQueryExamRecords.java —— 教师查看考试记录
- 支持按班级、试卷名筛选
- 分页显示所有学生的考试记录和成绩

### 3.3 其他 Action 文件（CRUD 类）

| 文件 | 功能 |
|------|------|
| `lwmAddstudent/lwmUpdatestudent/lwmstudentDelete/lwmquerystudent/lwmstudentExcel/lwmstudent_xx` | 学生增删改查+Excel导入导出 |
| `lwmAddteacher/lwmUpdateteacher/lwmteacherDelete/lwmqueryteacher/lwmteacherExcel/lwmteacher_xx` | 教师增删改查+Excel |
| `lwmAddsubject/lwmUpdatesubject/lwmsubjectDelete/lwmquerysubject/lwmsubjectExcel/lwmsubject_xx` | 科目增删改查+Excel |
| `lwmAddsourse/lwmUpdatecourse/lwmcourseDelete/lwmcourseExcel/lwmcourse_xx` | 课程（排课）增删改查+Excel |
| `lwmAddQuestion/lwmUpdateQuestion/lwmDeleteQuestion/lwmQueryQuestion/lwmLoadQuestions` | 试题库增删改查 |
| `lwmQueryPaper/lwmUpdatePaper/lwmDeletePaper/lwmViewPaper` | 试卷增删改查 |
| `lwmDeleteExamRecord` | 删除考试记录 |
| `lwmUpdateStudentAnswer` | 修改学生答案 |
| `lwmManageKnowledgePoint` | 知识点管理 |
| `lwmQuestionQualityAction` | 试题质量分析（难度/区分度） |
| `lwmKnowledgeMasteryAction` | 知识点掌握情况 |
| `lwmquerystuentcourseteacher` | 查询排课信息 |
| `lwmViewClassStudents` | 查看班级学生 |
| `SystemExit` | 系统退出（销毁 session） |

### 3.4 DAO 层核心文件

#### lwmpaperDAO.java
- `lwmAddPaper()`：插入试卷，返回自增ID
- `lwmExistPaperByNameSubject()`：检查重名试卷
- `lwmGetPaperQuestionIds()`：获取试卷包含的试题ID列表
- `lwmQueryByTeacherWithFilters()`：分页+筛选查询
- `hasSubmitRecord()`：判断是否有学生提交

#### lwmquestionDAO.java
- `lwmRandomPick()`：`ORDER BY RAND() LIMIT N` 随机抽题
- `lwmExistQuestionByContent()`：检查重复试题（相同科目+题型+内容）
- `lwmQueryBySubjectTypePaged()`：分页+多条件筛选
- 支持 `subjectIds` 用逗号分隔多科目查询（SQL IN 子句，有注入风险）

---

## 四、老师可能提出的问题及解答

### 基础概念类

**Q1：这个项目的整体架构是什么？用了什么设计模式？**
> 采用经典的三层架构（表现层/控制层/数据访问层）。JSP 负责视图，Servlet 负责控制，DAO 负责数据访问。使用了 DAO 设计模式将数据访问逻辑封装。

**Q2：为什么要用 PreparedStatement 而不是 Statement？**
> 1. **防止 SQL 注入**：参数化查询会自动转义特殊字符
> 2. **性能更好**：SQL 预编译后可重复使用
> 3. 代码更清晰可读

**Q3：MysqlConn 中 close() 方法的关闭顺序为什么是 rs → pstmt → conn？**
> 这是 JDBC 资源释放的规范顺序：先关结果集，再关语句对象，最后关连接。如果反过来关，可能导致结果集无法正常释放。

**Q4：WebServlet 注解的作用是什么？替代了什么？**
> `@WebServlet("/lwmLogin")` 等价于在 web.xml 中配置 `<servlet>` 和 `<servlet-mapping>`，是 Servlet 3.0+ 的简化写法。

**Q5：HttpSession 在这个项目中扮演什么角色？**
> Session 用于维持用户登录状态。登录成功后，用户对象（admin/student/teacher）存入 session，后续请求通过 `session.getAttribute()` 获取当前用户信息，实现身份认证。

### 数据库设计类

**Q6：试卷和试题是什么关系？如何实现的？**
> **多对多关系**。一张试卷包含多道试题，一道试题可出现在多张试卷中。通过中间表 `lwmpaperquestion` 实现，该表有两个外键分别关联试卷和试题表，并有唯一约束 `(lwmpaperid, lwmquestionid)` 防止重复加入。

**Q7：为什么试卷表要存各题型的数量、分值和题号，而不是只靠关联表？**
> 这是**冗余设计**，优点：
> 1. 查询试卷信息时不需要 JOIN 多表
> 2. 每张试卷的各题型分值可能不同，需要独立存储
> 3. 题号是展示顺序，关联表无法保证有序

**Q8：lwmstudentcourseteacher 表的作用是什么？**
> 这是排课表，记录**哪个班级的哪个科目由哪个老师教**。用于：
> 1. 试卷发布时限制教师只能发布到自己授课的班级
> 2. 学生只能看到本班被发布的试卷

**Q9：外键约束 ON DELETE CASCADE 和 ON DELETE SET NULL 的区别？在哪里用了？**
> - `CASCADE`：删除主表记录时自动删除关联的从表记录（如删科目时级联删除该科目的试题和试卷）
> - `SET NULL`：删除主表记录时将从表外键设为 NULL（如 `lwmexamscore` 删教师时保留成绩记录）
> - 项目中大量使用 CASCADE，仅 `lwmexamscore.lwmteacherid` 使用 SET NULL

### 业务逻辑类

**Q10：学生参加考试时有哪些安全控制？**
> 1. **登录校验**：从 Session 获取学生对象，null 则重定向到登录页
> 2. **班级校验**：学生所在班级必须在试卷发布的班级列表中
> 3. **时间校验**：当前时间必须在考试开始时间和结束时间之间
> 4. **超时防提交**：提交时检查学生实际考试时间是否超过试卷规定时长
> 5. **重复提交防**：通过 `lwmsubmitstatus` 状态位控制（0=草稿，1=已提交，2=已批阅）

**Q11：自动评分是如何实现的？**
> 在 `lwmGradeExam` 的 `autoGrade()` 方法中：
> - **单选/判断**：`studentAns.trim().equals(correctAns.trim())` 完全匹配 → 满分
> - **多选**：标准化处理（将 "A,C,D" 和 "ACD" 统一为数组），排序后逐项比较
> - **简答**：返回 0，需要教师手动评分

**Q12：错题本的自动记录机制是什么？**
> 在 `lwmSubmitScore.recordMistakes()` 中，提交成绩后自动执行：
> 1. 遍历每道题的得分
> 2. 比较 `score < maxScore`（该题满分）
> 3. 若得分低于满分 → 调用 `upsertMistake()` 插入或更新错题本
> 4. 使用 `ON DUPLICATE KEY UPDATE` 避免重复记录

**Q13：随机组卷是怎么实现的？**
> SQL 语句：`SELECT ... WHERE subjectId = ? AND questiontype = ? ORDER BY RAND() LIMIT N`
> MySQL 的 `RAND()` 函数随机排序，取前 N 条。缺点是数据量大时性能较差。

**Q14：分页功能是如何实现的？**
> `Fpage` 类核心计算：
> - `pageCount = (rowCount % pageSize == 0) ? rowCount/pageSize : rowCount/pageSize + 1`
> - `start = pageNow * pageSize`
> - SQL 使用 `LIMIT start, pageSize`（MySQL 分页语法）

**Q15：试卷发布时的班级校验逻辑是怎样的？**
> 1. POST 提交时，遍历用户勾选的班级
> 2. 查询 `lwmstudentcourseteacher` 检查该教师是否教授的该班级该科目
> 3. 不满足的班级被自动跳过并提示
> 4. 取消发布时检查是否有学生已提交（防止删除重要数据）

### 安全问题类

**Q16：这个项目存在哪些安全问题？**
> 1. **密码明文存储**：所有用户的密码都是明文存储在数据库中
> 2. **SQL 注入风险**：部分代码使用字符串拼接 SQL（如 `subjectIds IN (...)`），未完全参数化
> 3. **数据库密码硬编码**：MysqlConn.java 中直接写了 root/123456
> 4. **无 CSRF 防护**：表单提交无 Token 验证
> 5. **密码通过 GET/POST 明文传输**：无 HTTPS

**Q17：如何改进密码存储安全？**
> 使用 BCrypt 或 PBKDF2 等单向哈希算法对密码加密：
> ```java
> String hashedPwd = BCrypt.hashpw(password, BCrypt.gensalt());
> // 验证时
> BCrypt.checkpw(inputPwd, storedHash);
> ```

### 前端与交互类

**Q18：JSP 中 `<%=request.getAttribute("xxx")%>` 和 `${xxx}` 有什么区别？**
> - `<%=...%>` 是 JSP 脚本表达式，直接输出 Java 表达式的值
> - `${...}` 是 EL 表达式（Expression Language），更简洁
> - 项目中混用两种方式

**Q19：为什么很多地方用 `out.println("<script>alert(...)</script>")` 而不是 forward 到错误页面？**
> 因为要保留在原来的页面上，`alert()` 后 `history.go(-1)` 让用户留在表单页面修改输入。这是传统 Servlet 项目的常见做法。

### 项目相关深层问题

**Q20：如果要你在这个项目中增加"防止同一学生同一试卷多次提交"的功能，怎么做？**
> 在 `lwmSubmitExam.doPost()` 开头增加检查：
> ```java
> ResultSet rs = db.doQuery(
>     "SELECT COUNT(*) FROM lwmexamrecord WHERE lwmpaperid=? AND lwmstudentid=? AND lwmsubmitstatus IN (1,2)",
>     new Object[]{paperId, student.getLwmstudentid()});
> if (rs.next() && rs.getInt(1) > 0) {
>     out.println("<script>alert('您已提交过该试卷');</script>");
>     return;
> }
> ```

**Q21：成绩分析中的"及格线"和"优秀线"是怎么定义的？**
> - 及格线 = 试卷总分 × 60%
> - 优秀线 = 试卷总分 × 90%
> - 五段分布：不及格、及格、中等、良好、优秀（每段间隔总分的10%）

**Q22：为什么 lwmCreatePaper 中检查 `danxScore <= 0 || duoxScore <= 0 || pdScore <= 0 || jdScore <= 0` 有问题？**
> 这会导致即使用户只组单选题的试卷（其他题型数量为0）也必须给所有题型设置大于0的分值。这是一个逻辑 bug，应该改为只检查用户实际选择的题型的分值。

**Q23：DAO 中每个方法末尾都调用 `db.close()`，这是什么模式？有什么问题？**
> 每次 DAO 方法都 new 一个 `MysqlConn` 实例并使用后关闭。问题：
> 1. 频繁创建/销毁数据库连接开销大
> 2. 应该使用**连接池**（如 HikariCP、Druid）
> 3. 多个 DAO 操作无法在一个事务中完成（因为每个方法独立连接）

**Q24：知识点分析是如何关联到试题的？**
> 通过 `lwmquestionknowledge` 中间表关联试题和知识点。一道试题可关联多个知识点，一个知识点可属于多道试题。分析时 JOIN 了 4 张表：`lwmknowledgepoint → lwmquestionknowledge → lwmpaperquestion → lwmstudentanswer`。

**Q25：RAND() 排序抽题的优缺点？**
> 优点：实现简单
> 缺点：
> 1. 数据量大时性能差（全表排序）
> 2. 不能保证题目难度分布均衡
> 改进方案：使用 `WHERE id >= (SELECT FLOOR(RAND() * MAX(id))) LIMIT N` 或应用层随机

---

## 五、项目文件结构速查

```
src/main/java/com/example/lwmexam/
├── HelloServlet.java              # 初始模板（未使用）
├── action/lwmexam/                # 控制层（51个文件）
│   ├── lwmLogin.java              # ★ 三合一登录
│   ├── lwmTakeExam.java           # ★ 开始考试
│   ├── lwmSubmitExam.java         # ★ 提交考试
│   ├── lwmSaveExamDraft.java      # ★ 保存草稿
│   ├── lwmCreatePaper.java        # ★ 创建试卷
│   ├── lwmPublishPaper.java       # ★ 发布试卷
│   ├── lwmGradeExam.java          # ★ 批阅试卷
│   ├── lwmSubmitScore.java        # ★ 提交成绩
│   ├── lwmScoreAnalysisAction.java# ★ 成绩分析
│   ├── lwmKnowledgeAnalysisAction.java # ★ 知识点分析
│   ├── lwmMistakeBookAction.java  # ★ 错题本
│   ├── lwmRandomPickQuestions.java# 随机抽题AJAX
│   └── ...CRUD类（增删改查）
├── dao/lwmexam/                   # 数据访问层（8个文件）
│   ├── lwmpaperDAO.java           # 试卷DAO
│   ├── lwmquestionDAO.java        # 试题DAO
│   ├── lwmscoreDAO.java           # 成绩DAO
│   └── ...
├── entity/lwmexam/                # 实体类（12个文件）
├── service/lwmexam/               # 工具类（3个文件）
│   ├── MysqlConn.java             # 数据库连接
│   ├── Fpage.java                 # 分页
│   └── ExcelBook.java             # Excel工具
src/main/webapp/                    # JSP页面（约40个）
└── lwmexam.sql                     # 数据库脚本
```

---

## 六、考试快速回答模板

以下是最可能被问到的高频问题及简短回答版本：

| 问题 | 一句话答案 |
|------|-----------|
| 用什么框架？ | 原生 Servlet + JSP，无 Spring |
| 数据库连接怎么做的？ | JDBC 直连，PreparedStatement 防止注入 |
| 三个角色分别能做什么？ | 管理员管理基础数据；教师管理试题/试卷/阅卷；学生考试/查看成绩/错题本 |
| 怎么防止学生超时答题？ | 记录开始时间，提交时对比 `开始时间+考试时长` |
| 自动评分怎么实现的？ | 字符串比较（单选/判断），数组排序比较（多选） |
| 错题本怎么自动记录？ | 提交成绩时，得分 < 满分 → 自动加入错题本 |
| 分页怎么做的？ | LIMIT start, pageSize，pageSize=6 |
| 数据导入导出用什么？ | JExcelApi（jxl.jar） |
| 怎么防止 学生看不属于自己的试卷？ | Session 班级 + 试卷发布班级匹配 |
| 项目打包成什么？ | Maven war 包，部署到 Tomcat |

---

## 七、Bug 分析（按严重程度排序）

### 🔴 严重 Bug

#### Bug 1：创建/编辑试卷时强制所有题型分值 > 0（逻辑错误）

**位置**：`lwmCreatePaper.java:109-112`、`lwmUpdatePaper.java:128-131`

```java
if (danxScore <= 0 || duoxScore <= 0 || pdScore <= 0 || jdScore <= 0) {
```

使用 `||` 意味着只要任意一个题型分值为 0 就拒绝。如果用户只出了单选题，`duoxScore=0` → 条件成立 → 创建失败。

**正确做法**：只校验实际选择的题型，`if (danxNum > 0 && danxScore <= 0)`。

#### Bug 2：MysqlConn 线程不安全

**位置**：`MysqlConn.java:13-16`

```java
Connection conn = null;
PreparedStatement pstmt = null;
ResultSet rs = null;
```

这些是**实例变量**，DAO 类中 `MysqlConn db = new MysqlConn()` 同样声明为实例变量。Servlet 默认单例，并发请求会共享这些变量，一个请求的 `rs` 被另一个覆盖，导致数据混乱。

#### Bug 3：用 MAX(id) 获取自增 ID，并发下会取错

**位置**：`lwmpaperDAO.java:112`、`lwmAddQuestion.java:100`、`lwmSubmitExam.java:80`、`lwmSaveExamDraft.java:58`

```java
rs = db.doQuery("SELECT MAX(lwmquestionid) FROM ...", ...);
if (rs.next()) newQid = rs.getInt(1);
```

高并发时 MAX(id) 可能返回其他用户刚插入的 ID。应使用 JDBC 的 `Statement.RETURN_GENERATED_KEYS` + `getGeneratedKeys()`（`lwmTakeExam.java:120` 中正确使用了）。

#### Bug 4：SQL 注入风险

**位置**：`lwmquestionDAO.java:50`

```java
sql.append("AND q.lwmsubjectid IN (").append(subjectIds).append(") ");
```

`subjectIds` 直接拼接到 SQL 中，如果前端绕过校验传入恶意字符串即可注入。

#### Bug 5：试题质量分析的答案比对与评分逻辑不一致

**位置**：`lwmQuestionQualityAction.java:159-161`

```java
isCorrect = sAns != null && sAns.equalsIgnoreCase(cAns);
```

用原始字符串对比，但自动评分中对多选题做了排序+数组比较。结果 "A,C" 和 "C,A" 在这里被判定为错误，但实际阅卷时算正确。

#### Bug 6：lwmUpdatePaper 基本属性更新丢失字段

**位置**：`lwmUpdatePaper.java:150-161`

```java
try { paper.setLwmexamsore(Integer.parseInt(request.getParameter("lwmexamsore"))); } catch(Exception e) {}
int res = dao.lwmUpdatePaper(paper);
```

如果前端没传 `lwmexamsore`，parseInt 失败后总分变成默认值 0，UPDATE 覆盖了数据库中的正确值。

### 🟡 中等 Bug

#### Bug 7：lwmDeletePaper 无登录校验，可越权删除

**位置**：`lwmDeletePaper.java:14-31`

整个 `doGet` 方法没有任何 Session 校验！任何人知道试卷 ID 就可通过 URL 删除。对比其他 Action 都有 `if (teacher == null)` 检查。

#### Bug 8：lwmUpdatePaper POST 未校验教师权属

**位置**：`lwmUpdatePaper.java:58` 起（doPost）

GET 有校验，但 POST 中没检查 `teacher.getLwmteacherid() == paper.getLwmteacherid()`。任何教师可修改他人试卷。

#### Bug 9：DAO 无事务支持

所有 DAO 方法独立获取和关闭连接。如删除试卷时先删关联再删主表，第一步成功第二步失败会导致数据不一致。

#### Bug 10：密码明文存储 + 数据库凭据硬编码

全部用户密码明文存库；`MysqlConn.java` 多处 Action 中重复硬编码 `root/123456`。

#### Bug 11：质量分析中同一学生的重复答案被统计

**位置**：`lwmQuestionQualityAction.java:77-83`

```sql
SELECT ... FROM lwmstudentanswer sa WHERE sa.lwmpaperid = ?
```

如果学生对该试卷有多个 record（草稿+正式），该学生的答题会在统计中重复出现，影响难度/区分度计算。

#### Bug 12：ExcelBook finally 块 NPE

**位置**：`ExcelBook.java:96`

```java
} finally {
    bWorkbook.close();  // 如果构造失败 bWorkbook 为 null
}
```

### 🟢 轻微问题

| # | 位置 | 描述 |
|---|------|------|
| 13 | lwmLogin | 未校验 account/password 参数是否为空 |
| 14 | lwmAddQuestion | 未校验 questioncontent 是否为空 |
| 15 | Fpage.java:62 | `lwmstudentDAO hdao = new lwmstudentDAO()` 创建后从未使用 |
| 16 | lwmPublishPaper | 所有班级校验失败时 classname 被设为空字符串 |
| 17 | lwmViewClassStudents | classname 参数无空值校验，Mapper 拼接可能异常 |
| 18 | lwmTakeExam:95 | 又多处硬编码了数据库连接信息 |
| 19 | 全系统 | getWriter() 与 sendRedirect() 混用，异常时可能 IllegalStateException |
