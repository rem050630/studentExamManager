# 在线考试管理系统 — 全部代码逐文件详解

> 每个文件从三个角度讲解：**用途**（干什么）、**流程**（怎么工作的）、**核心代码**（是什么、在哪里、怎么实现的）

---

# 第一部分：基础设施层（3个文件）

---

## 1. MysqlConn.java

### 用途
为整个项目提供统一的MySQL数据库连接和执行SQL的能力。所有DAO和部分Servlet通过它操作数据库，是项目的数据访问基石。

### 流程
1. `getConnection()` 加载MySQL驱动 → `DriverManager.getConnection()` 建立TCP连接
2. `doQuery(sql, params)` 先调 `getConnection()` → `prepareStatement(sql)` → 循环 `setObject(i+1, param[i])` 绑定参数 → `executeQuery()` 返回 `ResultSet`
3. `doUpdate(sql, params)` 同上但调用 `executeUpdate()` 返回影响行数 `int`
4. `close()` 依次关闭 `ResultSet` → `PreparedStatement` → `Connection`

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/service/lwmexam/MysqlConn.java`

**是什么**：一个纯JDBC工具类，4个字段 + 4个方法

**怎么实现的**：
```java
// 第10行 — 连接字符串（GBK编码，MySQL 8.0）
String url = "jdbc:mysql://localhost:3306/lwmexam?serverTimezone=UTC&useUnicode=true&characterEncoding=gbk&mysqlEncoding=utf8";
String user = "root";
String password = "123456";

// 第27-38行 — 参数化查询（防止SQL注入的关键）
public ResultSet doQuery(String sql, Object[] param) {
    this.getConnection();                          // 每次建新连接
    pstmt = this.conn.prepareStatement(sql);       // 预编译SQL
    for (int i = 0; i < param.length; i++)
        pstmt.setObject(i + 1, param[i]);          // 参数绑定（1-based索引）
    rs = pstmt.executeQuery();
    return rs;
}

// 第40-52行 — 更新操作（INSERT/UPDATE/DELETE）
public int doUpdate(String sql, Object[] param) {
    this.getConnection();
    pstmt = this.conn.prepareStatement(sql);
    for (int i = 0; i < param.length; i++)
        pstmt.setObject(i + 1, param[i]);
    res = pstmt.executeUpdate();                   // 返回影响行数
    return res;
}
```
**关键点**：每次调用都新建连接→执行→调用方手动close()。无连接池。`PreparedStatement.setObject()` 实现参数化查询，是防止SQL注入的核心机制。

---

## 2. Fpage.java

### 用途
为所有列表页面提供统一的分页计算。接收COUNT查询SQL，自动算出总页数、起始偏移量，输出给JSP分页组件 `lwmfoot.jsp` 渲染导航条。

### 流程
1. 调用方设置 `pageNow`（从URL参数 `?page=N` 读取）
2. 调用 `setFpage(countSql, params)` → 内部执行 `SELECT COUNT(*)` → 获取 `rowCount`
3. 自动计算 `pageCount = ceil(rowCount / pageSize)` 和 `start = pageNow * pageSize`
4. 调用方用 `start` 和 `pageSize` 拼接 `LIMIT start, pageSize` 查询分页数据
5. JSP读取 `${fp.pageCount}`、`${fp.rowCount}`、`${fp.pageNow}` 渲染分页条

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/service/lwmexam/Fpage.java`

**是什么**：5个私有字段 + getter/setter + 自动计算逻辑

**怎么实现的**：
```java
// 第9-13行 — 核心配置字段
private int pageSize = 6;    // 每页6条（全系统统一）
private int pageNow = 0;     // 0-based当前页索引
private int start = 0;       // SQL: LIMIT start, pageSize
private int pageCount = 0;   // 总页数
private int rowCount = 0;    // 总记录数

// 第51-58行 — rowCount改变时自动重算pageCount和start
public void setRowCount(int rowCount) {
    this.rowCount = rowCount;
    if (rowCount % pageSize == 0)
        pageCount = rowCount / pageSize;           // 整除
    else
        pageCount = rowCount / pageSize + 1;       // 有余数则+1
    start = pageNow * pageSize;                    // 核心公式
}

// 第60-77行 — 执行COUNT查询获取rowCount
public void setFpage(String sql, Object[] param) {
    start = pageNow * pageSize;
    MysqlConn db = new MysqlConn();
    rs = db.doQuery(sql, param);                   // 执行 SELECT COUNT(*)
    if (rs.next()) rowCount = rs.getInt(1);        // 取第一列
    // 同上计算pageCount
    db.close();
}
```
**关键点**：`start = pageNow * pageSize` 是分页的核心公式。`pageNow` 是0-based（第0页=首页），JSP显示时转换为1-based（`pageNow+1`）。

---

## 3. ExcelBook.java

### 用途
使用JExcelAPI库（`jxl.*`）实现Excel文件的读写。`excelOut()` 将数据库查询结果导出为 `.xls` 文件并触发下载，`excelIn()` 解析 `.xls` 返回 `ArrayList<String[]>`。

### 流程
**导出**：接收文件路径 + ResultSet + 列头数组 → 创建 `WritableWorkbook` → 写表头行 → 逐行逐列写数据（`rs.getString(i+1)`） → `write()` + `close()`

**导入**：接收文件路径 → 打开 `Workbook` → 读第一个Sheet → 双层循环（行×列）→ `getContents()` 取单元格值 → 收集到 `ArrayList<String[]>`

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/service/lwmexam/ExcelBook.java`

**是什么**：3个方法（`excelOut`、`excelIn`、`main`测试），依赖 `jxl.jar`

**怎么实现的**：
```java
// excelOut核心 — 写Excel
WritableWorkbook bWorkbook = Workbook.createWorkbook(new File(path));
WritableSheet sheet = bWorkbook.createSheet("sheet1", 0);
// 写表头
for (int i = 0; i < params.length; i++)
    sheet.addCell(new Label(i, 0, params[i].toString()));
// 写数据行
int row = 1;
while (rs.next()) {
    for (int i = 0; i < params.length; i++)
        sheet.addCell(new Label(i, row, rs.getString(i + 1)));  // ResultSet列从1开始
    row++;
}
bWorkbook.write();
bWorkbook.close();

// excelIn核心 — 读Excel
Workbook workbook = Workbook.getWorkbook(new File(path));
Sheet sheet = workbook.getSheet(0);                // 只读第一个Sheet
for (int i = 0; i < sheet.getRows(); i++) {
    String[] row = new String[sheet.getColumns()];
    for (int j = 0; j < sheet.getColumns(); j++)
        row[j] = sheet.getCell(j, i).getContents(); // Cell: (列,行), 0-based
    list.add(row);
}
workbook.close();
```
**关键点**：导出时ResultSet列从1开始，导入时Cell列从0开始，需要注意索引偏移。

---

# 第二部分：实体层（13个POJO）

所有实体都是纯POJO，手写getter/setter，无框架注解。每个实体对应一张数据库表。

---

## 4. lwmAdmin.java — 管理员实体

### 用途
映射 `lwmadmin` 表，存储管理员登录凭据和姓名。登录验证时从此表查询。

### 流程
Servlet从数据库查出ResultSet → 逐列取值 → `setXxx()` 填充字段 → 存入session

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmAdmin.java`

**怎么实现的**：4个private字段（`lwmadminid:int`、`lwmadminaccount:String`、`lwmadminpassword:String`、`lwmadminname:String`）+ 4对getter/setter。无业务逻辑。

---

## 5. lwmStudent.java — 学生实体

### 用途
映射 `lwmstudent` 表，承载学生全部身份信息。用于登录验证、班级归属判断（`FIND_IN_SET`）、考试记录关联。session中存此对象代表学生登录态。

### 流程
登录时从 `lwmstudent` 表查7个字段填充 → 存入session → 所有学生端页面从session读取

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmStudent.java`

**怎么实现的**：8个private字段（`lwmstudentid:int`、`lwmstudentno:String`学号、`lwmstudentname:String`、`lwmstudentpassword:String`、`lwmgender:String`、`lwmgrade:String`年级、`lwmmajor:String`专业、`lwmclassname:String`班级名）+ 8对getter/setter。

**关键字段**：`lwmclassname` 是班级归属的核心字段，在 `lwmTakeExam` 中与学生班级做精确比对。

---

## 6. lwmTeacher.java — 教师实体

### 用途
映射 `lwmteacher` 表，承载教师身份信息。session中存此对象代表教师登录态，`lwmteacherid` 用于试卷所有权验证、排课查询、阅卷记录。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmTeacher.java`
**怎么实现的**：6个字段（id、工号、姓名、密码、性别、电话）+ 6对getter/setter。

---

## 7. lwmSubject.java — 课程/科目实体

### 用途
映射 `lwmexamsubject` 表，存储科目元数据。是全系统最广泛使用的实体——试题、试卷、排课、知识点全部关联到科目。`lwmQueryAllSubject()` 是出现频率最高的方法，几乎所有下拉框都调用它。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmSubject.java`
**怎么实现的**：5个字段（`lwmsubjectid:Integer`、`lwmsubjectname:String`、`lwmsubjectdesc:String`描述/代码、`lwmsubjectscore:Integer`学分、`lwmterm:String`学期）。注意id和score使用 `Integer` 包装类型（可为null），其余用基本类型。

---

## 8. lwmExamQuestion.java — 试题实体

### 用途
映射 `lwmexamquestion` 表，存储题库中每一道试题。支持4种题型，正确答案格式因题型而异：单选 `"A"`、多选 `"A,C"`、判断 `"对"/"错"`、简答自由文本。`lwmsubjectname` 是JOIN展示字段（非数据库列）。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmExamQuestion.java`
**怎么实现的**：10个字段（id、subjectId、questiontype题型、questioncontent题目正文、optiona/b/c/d四个选项、correctanswer正确答案）+ JOIN字段 `lwmsubjectname`。10对getter/setter。

---

## 9. lwmExamPaper.java — 试卷实体（最复杂实体）

### 用途
映射 `lwmexampaper` 表，存储试卷完整配置。24个字段包含四种题型的数量/分值/题号列表、考试时间窗口、时长、总分、发布班级（逗号分隔）。是题库与考试流程之间的枢纽。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmExamPaper.java`
**怎么实现的**：24个字段分为4组——
- 基本信息：`lwmpaperid`、`lwmpapername`、`lwmsubjectid`、`lwmexamtime`(时长分钟)、`lwmexamsore`(总分)、`lwmstarttime`、`lwmendtime`、`lwmteacherid`、`lwmclassname`(发布班级逗号分隔)
- 单选题配置：`lwmdanxnum`(数量)、`lwmdanxscore`(每题分)、`lwmdanxnos`(题号列表如"1,2,3")
- 多选题配置：`lwmduoxnum/score/nos`
- 判断题配置：`lwmpdnum/score/nos`
- 简答题配置：`lwmjdnum/score/nos`
- JOIN字段：`lwmsubjectname`、`lwmteachername`

**设计要点**：`lwmdanxnos` 等字段存储的是题号列表（如 `"1,3,5"` 表示在试卷中第1、3、5题是单选题），不是试题ID。试题ID通过 `lwmpaperquestion` 关联表获取。

---

## 10. lwmExamRecord.java — 考试记录实体

### 用途
映射 `lwmexamrecord` 表，记录每次学生参加考试的实例。**状态机**：`lwmsubmitstatus`（0=草稿考试中 → 1=已提交待阅卷 → 2=已评分）。`lwmstarttime` 是倒计时的基准时间。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmExamRecord.java`
**怎么实现的**：6个核心字段（recordid、paperid、studentid、starttime、endtime、submitstatus）+ 4个JOIN展示字段（学号、姓名、班级、试卷名）。

---

## 11. lwmStudentAnswer.java — 学生答卷实体

### 用途
映射 `lwmstudentanswer` 表，存储学生每道题的作答内容和得分。每个 `(recordId, questionId)` 对应一条记录。JOIN了大量试题字段用于阅卷页面展示（题型、选项、正确答案等15个字段）。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmStudentAnswer.java`
**怎么实现的**：7个核心字段（answerid、recordid、questionid、studentanswer学生答案、questionscore得分、studentid、paperid）+ 8个JOIN展示字段。15对getter/setter。

---

## 12. lwmExamScore.java — 考试成绩实体

### 用途
映射 `lwmexamscore` 表，存储学生某次考试的总分和阅卷教师。使用 `ON DUPLICATE KEY UPDATE` 确保一个recordId只有一条成绩记录。

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/entity/lwmexam/lwmExamScore.java`
**怎么实现的**：7个核心字段（scoreid、recordid、totalscore总分、teacherid阅卷教师、scoretime评分时间、studentid、paperid）+ 3个JOIN字段。

---

## 13-16. 其余4个实体（简要）

| 文件 | 用途 | 关键字段 |
|------|------|---------|
| **lwmKnowledgePoint.java** | 映射 `lwmknowledgepoint` 表，存储各科目的知识点 | kpid、subjectid、kpname、kpdesc |
| **lwmQuestionKnowledge.java** | 映射 `lwmquestionknowledge` 多对多中间表 | qkid、questionid(FK)、kpid(FK)、UNIQUE约束 |
| **lwmMistakeBook.java** | 映射 `lwmmistakebook` 表，记录错题和复习状态 | mid、studentid、questionid、iswrong、reviewstatus(0/1/2) |
| **lwmstudentcourseteacher.java** | 映射排课三向关联表（班级-科目-教师-学期） | sctid、classname、subjectid、teacherid、semester |

---

# 第三部分：数据访问层（9个DAO）

所有DAO遵循统一模式：内部 `new MysqlConn()` → 每方法 "建连→执行SQL→关闭"。使用 `PreparedStatement` 参数化查询防止SQL注入。

---

## 17. lwmstudentDAO.java

### 用途
封装 `lwmstudent` 表全部CRUD + 模糊搜索 + 班级列表查询。管理员端学生管理的数据层。

### 流程
每个方法：创建MysqlConn → 拼SQL → `db.doQuery/doUpdate(sql, params)` → `db.close()` → 返回结果

### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/dao/lwmexam/lwmstudentDAO.java`

**怎么实现的（关键方法）**：

```java
// lwmSearchStudent — 5字段LIKE模糊搜索
// 位置：约第30行
public List<lwmStudent> lwmSearchStudent(String keyword) {
    String sql = "SELECT * FROM lwmstudent WHERE lwmstudentno LIKE ? " +
                 "OR lwmstudentname LIKE ? OR lwmgrade LIKE ? " +
                 "OR lwmmajor LIKE ? OR lwmclassname LIKE ?";
    String kw = "%" + keyword + "%";               // 模糊匹配包装
    Object[] param = {kw, kw, kw, kw, kw};          // 5个参数都是同一个keyword
    return lwmQuerySomeStudent(sql, param);
}

// lwmQuerySomeStudent — 通用查询（供分页和其他灵活查询使用）
// 位置：约第20行
// 用ResultSet逐列取值构建List<lwmStudent>，按固定列顺序映射
while (rs.next()) {
    lwmStudent s = new lwmStudent();
    s.setLwmstudentid(rs.getInt("lwmstudentid"));
    s.setLwmstudentno(rs.getString("lwmstudentno"));
    // ...映射全部8个字段
    list.add(s);
}
```

**设计特点**：`lwmQuerySomeStudent(sql, params)` 是一个通用查询方法，允许调用方传入任意SQL，实现了DAO的灵活性——不只是固定查询，支持分页SQL和动态WHERE条件。

---

## 18-20. lwmTeacherDAO、lwmsubjectDAO、lwmCourseArrangeDAO（3个类似结构）

### 共同用途
封装各自表的CRUD操作，结构与学生DAO一致：通用查询方法 + 固定CRUD方法 + COUNT统计。

### lwmCourseArrangeDAO 的特殊方法

```java
// 排重检查 — 防止同班级+同科目+同学期重复安排教师
// 位置：lwmCourseArrangeDAO.java 约第70行
public boolean lwmExistArrange(String lwmclassname, int lwmsubjectid,
                                String lwmsemester, int excludeId) {
    String sql = "SELECT COUNT(*) FROM lwmstudentcourseteacher " +
                 "WHERE lwmclassname=? AND lwmsubjectid=? AND lwmsemester=? AND lwmsctid!=?";
    // excludeId参数用于编辑时排除自身
    Object[] param = {lwmclassname, lwmsubjectid, lwmsemester, excludeId};
    ResultSet rs = db.doQuery(sql, param);
    return rs.next() && rs.getInt(1) > 0;
}
```

---

## 21. lwmquestionDAO.java — 试题数据访问（核心DAO）

### 用途
封装试题的全部CRUD、组合筛选、随机抽取、重复检查。是题库模块的数据中枢，被11个Servlet调用。

### 核心代码

**在哪里**：`src/main/java/com/example/lwmexam/dao/lwmexam/lwmquestionDAO.java`

**关键方法怎么实现的**：

```java
// 1. 随机抽题 — ORDER BY RAND() LIMIT n（位置约第100行）
public List<lwmExamQuestion> lwmRandomPick(int subjectId, String questiontype, int count) {
    String sql = "SELECT q.*, s.lwmsubjectname FROM lwmexamquestion q " +
                 "LEFT JOIN lwmexamsubject s ON q.lwmsubjectid = s.lwmsubjectid " +
                 "WHERE q.lwmsubjectid = ? AND q.lwmquestiontype = ? " +
                 "ORDER BY RAND() LIMIT ?";           // MySQL随机排序
    return lwmQuerySomeQuestion(sql, new Object[]{subjectId, questiontype, count});
}

// 2. 组合筛选 — 动态构建WHERE条件（位置约第50行）
public List<lwmExamQuestion> lwmQueryBySubjectType(
        String subjectIds, String questiontype, String keyword) {
    StringBuilder sql = new StringBuilder(
        "SELECT q.*, s.lwmsubjectname FROM lwmexamquestion q " +
        "LEFT JOIN lwmexamsubject s ON q.lwmsubjectid = s.lwmsubjectid WHERE 1=1");
    List<Object> params = new ArrayList<>();
    if (subjectIds != null && !subjectIds.isEmpty()) {
        sql.append(" AND q.lwmsubjectid IN (").append(subjectIds).append(")"); // IN子句
    }
    if (questiontype != null && !questiontype.isEmpty()) {
        sql.append(" AND q.lwmquestiontype = ?");
        params.add(questiontype);
    }
    if (keyword != null && !keyword.isEmpty()) {
        sql.append(" AND q.lwmquestioncontent LIKE ?");
        params.add("%" + keyword + "%");
    }
    sql.append(" ORDER BY q.lwmquestionid DESC");
    return lwmQuerySomeQuestion(sql.toString(), params.toArray());
}

// 3. 重复检查 — 全字段精确匹配（位置约第120行）
public boolean lwmExistQuestion(lwmExamQuestion q, int excludeId) {
    String sql = "SELECT COUNT(*) FROM lwmexamquestion WHERE lwmsubjectid=? " +
                 "AND lwmquestiontype=? AND lwmquestioncontent=? " +
                 "AND lwmoptiona=? AND lwmoptionb=? AND lwmoptionc=? AND lwmoptiond=? " +
                 "AND lwmcorrectanswer=? AND lwmquestionid!=?";  // 排除自身（编辑时）
}
```

---

## 22. lwmpaperDAO.java — 试卷数据访问（最复杂DAO）

### 用途
封装 `lwmexampaper` 表和 `lwmpaperquestion` 关联表的所有操作 + 两个安全保护查询方法。

### 核心代码

**在哪里**：`src/main/java/com/example/lwmexam/dao/lwmexam/lwmpaperDAO.java`

**关键方法怎么实现的**：

```java
// 1. 添加试卷并返回新ID — INSERT + SELECT MAX
// 位置约第60行
public int lwmAddPaper(lwmExamPaper p) {
    db.doUpdate("INSERT INTO lwmexampaper(lwmpapername,lwmsubjectid,...[20个字段]) VALUES(?,?,...)", params);
    // 通过MAX取刚插入的ID（非线程安全，但在教学系统中可接受）
    ResultSet rs = db.doQuery(
        "SELECT MAX(lwmpaperid) FROM lwmexampaper WHERE lwmteacherid=? AND lwmpapername=?",
        new Object[]{p.getLwmteacherid(), p.getLwmpapername()});
    return rs.next() ? rs.getInt(1) : 0;
}

// 2. 安全检查：是否有学生提交（位置约第200行）
public boolean hasSubmitRecord(int paperId) {
    ResultSet rs = db.doQuery(
        "SELECT COUNT(*) FROM lwmexamrecord WHERE lwmpaperid = ? AND lwmsubmitstatus IN (1, 2)",
        new Object[]{paperId});
    return rs.next() && rs.getInt(1) > 0;
}

// 3. 试题引用检查：哪些试卷用了该试题（位置约第210行）
public List<lwmExamPaper> getPapersByQuestionId(int questionId) {
    String sql = "SELECT p.* FROM lwmexampaper p " +
                 "INNER JOIN lwmpaperquestion pq ON p.lwmpaperid = pq.lwmpaperid " +
                 "WHERE pq.lwmquestionid = ?";
    return lwmQuerySomePaper(sql, new Object[]{questionId});
}

// 4. 级联删除：先删关联再删主表（位置约第170行）
public int lwmDeletePaper(int paperId) {
    lwmDeletePaperQuestions(paperId);    // DELETE FROM lwmpaperquestion WHERE lwmpaperid=?
    return db.doUpdate("DELETE FROM lwmexampaper WHERE lwmpaperid = ?", new Object[]{paperId});
}
```

---

## 23-25. lwmscoreDAO、lwmKnowledgePointDAO、lwmMistakeBookDAO（3个）

### lwmscoreDAO.java 核心方法

```java
// 加载答卷详情 — 阅卷页面的数据源，JOIN试题表获取题目信息
public List<lwmStudentAnswer> lwmQueryAnswersByRecord(int recordId) {
    String sql = "SELECT sa.*, q.lwmquestiontype, q.lwmquestioncontent, " +
                 "q.lwmoptiona, q.lwmoptionb, q.lwmoptionc, q.lwmoptiond, " +
                 "q.lwmcorrectanswer FROM lwmstudentanswer sa " +
                 "JOIN lwmexamquestion q ON sa.lwmquestionid = q.lwmquestionid " +
                 "WHERE sa.lwmrecordid = ?";
}

// 保存总分 — ON DUPLICATE KEY UPDATE 确保一条成绩记录
public int lwmSaveScore(lwmExamScore score) {
    String sql = "INSERT INTO lwmexamscore(lwmrecordid,lwmtotalscore,lwmteacherid,lwmstudentid,lwmpaperid) " +
                 "VALUES(?,?,?,?,?) ON DUPLICATE KEY UPDATE lwmtotalscore=?, lwmteacherid=?";
}
```

### lwmKnowledgePointDAO.java 核心方法

```java
// 保存试题的知识点关联 — "先删后插"策略
public void saveQuestionKPs(int questionId, int[] kpIds) {
    db.doUpdate("DELETE FROM lwmquestionknowledge WHERE lwmquestionid = ?", new Object[]{questionId});
    for (int kpId : kpIds) {
        db.doUpdate("INSERT INTO lwmquestionknowledge(lwmquestionid,lwmkpid) VALUES(?,?)",
            new Object[]{questionId, kpId});
    }
}

// 安全检查：知识点被试题引用次数
public int countQuestionsByKP(int kpId) {
    ResultSet rs = db.doQuery("SELECT COUNT(*) FROM lwmquestionknowledge WHERE lwmkpid = ?", new Object[]{kpId});
    return rs.next() ? rs.getInt(1) : 0;
}
```

### lwmMistakeBookDAO.java 核心方法

```java
// 错题记录 — INSERT ON DUPLICATE KEY UPDATE（存在则更新，不存在则插入）
public void upsertMistake(int studentId, int questionId, boolean isWrong) {
    String sql = "INSERT INTO lwmmistakebook(lwmstudentid,lwmquestionid,lwmiswrong,lwmreviewstatus,lwmlastupdatetime) " +
                 "VALUES(?,?,?,0,NOW()) ON DUPLICATE KEY UPDATE lwmiswrong=?, lwmlastupdatetime=NOW()";
    db.doUpdate(sql, new Object[]{studentId, questionId, isWrong ? 1 : 0, isWrong ? 1 : 0});
}
```

---

# 第四部分：控制层 — 55个Servlet

---

## 4.1 登录与退出（2个）

---

### 26. lwmLogin.java — `/lwmLogin`

#### 用途
处理三种角色（管理员/教师/学生）的登录验证。根据用户选择的role去对应表中比对账号密码，验证成功后存入session并重定向。

#### 流程
1. 接收 `account` + `password` + `role` 三个表单参数
2. 根据 `role` 值分支：
   - `"admin"` → `SELECT * FROM lwmadmin WHERE lwmadminaccount=? AND lwmadminpassword=?`
   - `"student"` → `SELECT * FROM lwmstudent WHERE lwmstudentno=? AND lwmstudentpassword=?`
   - `"teacher"` → `SELECT * FROM lwmteacher WHERE lwmteacherno=? AND lwmteacherpassword=?`
3. `rs.next()` 为true → 从ResultSet逐列取值填充实体 → `session.setAttribute("admin"/"student"/"teacher", entity)` + `session.setAttribute("role", role)` → `response.sendRedirect("xxx_main.jsp")`
4. `rs.next()` 为false → `out.println("<script>alert('xxx错误！');history.go(-1);</script>")`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmLogin.java`

**怎么实现的**：
```java
// 第38-56行 — 管理员分支
if ("admin".equals(role)) {
    ResultSet rs = db.doQuery(
        "SELECT * FROM lwmadmin WHERE lwmadminaccount=? AND lwmadminpassword=?",
        new Object[]{account, password});
    if (rs.next()) {
        lwmAdmin admin = new lwmAdmin();
        admin.setLwmadminid(rs.getInt("lwmadminid"));
        // ...逐列取值填充
        session.setAttribute("admin", admin);
        session.setAttribute("role", "admin");
        response.sendRedirect("lwmadmin_main.jsp");
    } else {
        out.println("<script>alert('管理员账号或密码错误！');history.go(-1);</script>");
    }
}
// 学生和教师分支结构相同，只是表名、实体类、redirect目标不同
```

**关键点**：
- 为什么不用DAO而直接用MysqlConn：保持了与现有DAO相同的JDBC模式
- 密码明文对比（`lwmadminpassword=?`）——教学项目可接受，生产环境必须加密
- 管理员从 `lwmadminaccount`（账号）查询，学生从 `lwmstudentno`（学号）查询，教师从 `lwmteacherno`（工号）查询

---

### 27. SystemExit.java — `/SystemExit`

#### 用途
注销登录。销毁当前HTTP Session → 重定向到登录页。

#### 流程
`request.getSession().invalidate()` → `response.sendRedirect("login.jsp")`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/SystemExit.java`
**怎么实现的**：3行核心代码。doGet和doPost都执行同样逻辑。所有角色共用此退出接口，页面通过 `<a href="SystemExit" target="_parent">` 调用。

---

## 4.2 管理员-学生CRUD（6个Servlet）

---

### 28. lwmAddstudent.java — `/lwmAddstudent`

#### 用途
处理添加学生的表单提交。从 `lwmaddstudent.jsp` 接收7个字段 → DAO写入 `lwmstudent` 表。

#### 流程
1. 设置编码UTF-8
2. 收集7个参数：`lwmstudentno`、`lwmstudentname`、`lwmstudentpassword`、`lwmgender`、`lwmgrade`、`lwmmajor`、`lwmclassname`
3. `new lwmStudent()` + 逐字段 `setXxx()`
4. `lwmstudentDAO.lwmAddStudent(student)` → INSERT
5. 返回值>0 → `alert('添加成功')` + `location.href='lwmstudent_xx'`
6. 返回值≤0 → `alert('添加失败，请检查学号是否已存在')` + `history.go(-1)`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmAddstudent.java`
**怎么实现的**：标准CRUD模式 — 收集参数 → 构建实体 → DAO插入 → JS alert结果

**注意**：学号唯一性依赖数据库UNIQUE约束报错（没有显式查重逻辑）。

---

### 29. lwmquerystudent.java — `/lwmquerystudent`

#### 用途
处理学生搜索请求。接收keyword → `lwmstudentDAO.lwmSearchStudent(keyword)` 5字段LIKE → forward到 `lwmstudentlist.jsp`。

#### 流程
读取 `keyword`（空则默认 `""`） → DAO.search → `request.setAttribute("someStudent", list)` → `request.getRequestDispatcher("lwmstudentlist.jsp").forward(request, response)`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmquerystudent.java`
**关键区别**：使用 **forward**（不是redirect），数据通过 `request.setAttribute` 传递，JSP通过 `${someStudent}` 接收。

---

### 30. lwmstudent_xx.java — `/lwmstudent_xx`

#### 用途
学生分页列表。使用Fpage分页（每页6条），支持 `tj` 关键字搜索，按 `lwmstudentid DESC` 排序。

#### 流程
1. 读 `tj` 搜索参数 → 拼LIKE SQL → 执行COUNT → `Fpage.setRowCount()`
2. 读 `page` 参数 → 设置 `Fpage.pageNow` → 计算 `start = pageNow * 6`
3. 拼分页SQL：`SELECT * FROM lwmstudent WHERE ... LIKE ... ORDER BY lwmstudentid DESC LIMIT start, 6`
4. `session.setAttribute("someStudent", list)` + `session.setAttribute("fp", fpage)` + `session.setAttribute("tj", tj)`
5. `response.sendRedirect("lwmstudentlist.jsp")`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmstudent_xx.java`
**关键区别**：使用 **session + redirect**（不是request + forward），因为redirect会丢失request属性。

---

### 31. lwmUpdatestudent.java — `/lwmUpdatestudent`

#### 用途
双用途Servlet：doGet按ID加载学生信息 → forward编辑页；doPost接收修改后表单 → 更新数据库。

#### 流程
**doGet**：读 `id` 参数 → `lwmstudentDAO.lwmQueryStudentById(id)` → `request.setAttribute("student", entity)` → forward `lwmupdatestudent.jsp`

**doPost**：收集8个参数（含 `lwmstudentid`） → 构建实体 → `lwmstudentDAO.lwmUpdateStudent(student)` → alert结果 → redirect回自身GET

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmUpdatestudent.java`
**关键设计**：POST成功后redirect回GET（`lwmUpdatestudent?id=xxx`），这样刷新页面不会重复提交。

---

### 32. lwmstudentDelete.java — `/lwmstudentDelete`

#### 用途
按ID删除学生记录。通过URL `?id=xxx` 触发（GET方式）。

#### 流程
读 `id` 参数 → `lwmstudentDAO.lwmDeleteStudentById(id)` → alert结果 → redirect `lwmstudent_xx`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmstudentDelete.java`
**注意**：使用doGet处理删除操作（非RESTful风格），页面通过 `<a href="lwmstudentDelete?id=xxx" onclick="return confirm('确认删除吗?')">` 调用。

---

### 33. lwmstudentExcel.java — `/lwmstudentExcel`

#### 用途
学生数据的Excel导入导出。doGet导出全部学生为 `.xls` 下载；doPost上传 `.xls` 批量导入。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmstudentExcel.java`
**怎么实现的**：
- **导出**：`MysqlConn` 查询全部学生 → `ExcelBook.excelOut("D:/学生信息.xls", rs, headers)` → `SmartUpload` 下载文件 → 删除临时文件
- **导入**：`SmartUpload` 接收上传 → 保存到 `D:/学生信息.xls` → `ExcelBook.excelIn()` 解析 → 循环 `INSERT INTO lwmstudent VALUES(?,?,?,?,?,?,?,?)` → alert结果

---

## 4.3-4.5 管理员-教师/课程/排课CRUD（每个模块5-6个Servlet）

三类管理模块与学生CRUD结构完全一致，仅操作的表和实体不同。不再重复描述流程，仅列出各文件的区别点：

| Servlet | 操作的DAO/表 | 特殊逻辑 |
|---------|-------------|---------|
| 34. lwmAddteacher | lwmTeacherDAO / lwmteacher | 5字段（工号/姓名/密码/性别/电话） |
| 38. lwmteacherDelete | lwmTeacherDAO | 无额外检查 |
| 40. lwmAddsubject | lwmsubjectDAO / lwmexamsubject | 4字段（名称/描述/学分/学期） |
| 46. lwmAddsourse | lwmCourseArrangeDAO | **INSERT前调用 `lwmExistArrange()` 排重** |
| 49. lwmUpdatecourse | lwmCourseArrangeDAO | **doPost更新前再次排重（排除自身ID）** |

---

## 4.6 教师-题库管理（5个Servlet）

---

### 52. lwmQueryQuestion.java — `/lwmQueryQuestion`

#### 用途
教师题库列表，**按教师所教科目范围过滤试题**，支持科目+题型+关键字三级筛选，Fpage分页。

#### 流程
1. 验证teacher登录
2. 查排课表获取该教师所有科目 → `Set<Integer>` 去重 → 拼成 `"1,2,3"` 格式的 `subjectIds`
3. 构建 `subjectList`（[id, name] 数组列表）供下拉框
4. 读取筛选参数：`questiontype`、`keyword`、`subjectid`（单选科目时覆盖默认的IN子句）
5. 构建 `tj` 参数字符串（用于分页链接保持筛选条件）
6. `lwmquestionDAO.lwmCountByFilters()` + `lwmQueryBySubjectTypePaged()` 分页查询
7. 设置7个request属性 → forward `lwmteacher_question_list.jsp`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmQueryQuestion.java`

**怎么实现的**：
```java
// 关键：从排课表构建教师的科目ID列表（控制试题可见范围）
List<lwmstudentcourseteacher> courses = courseDao.lwmQuerySomeSct(
    "SELECT sct.*, sub.lwmsubjectname, tea.lwmteachername FROM lwmstudentcourseteacher sct " +
    "LEFT JOIN lwmexamsubject sub ON sct.lwmsubjectid = sub.lwmsubjectid " +
    "LEFT JOIN lwmteacher tea ON sct.lwmteacherid = tea.lwmteacherid " +
    "WHERE sct.lwmteacherid = ?",
    new Object[]{teacher.getLwmteacherid()});

// 去重科目ID，拼成 "1,2,3" 用于SQL IN子句
String subjectIds = courses.stream()
    .map(c -> String.valueOf(c.getLwmsubjectid()))
    .distinct()
    .collect(Collectors.joining(","));
```

**数据权限核心**：通过排课表限制教师只能看到自己教的科目的试题。

---

### 53. lwmAddQuestion.java — `/lwmAddQuestion`

#### 用途
处理新增试题表单。收集参数 → 表单验证 → 重复检查 → INSERT → 保存知识点关联。

#### 流程
1. 验证teacher登录
2. 收集参数（subjectId、questionType、content、optionA-D、correctAnswer数组）
3. 正确答案处理：`String.join(",", request.getParameterValues("lwmcorrectanswer"))` — 多选时拼接为 "A,C"
4. 验证必填：单选/多选题四个选项必须都有值；正确答案必须选择
5. 重复检查 ×2：`lwmExistQuestionByContent(q)`（内容重复）+ `lwmExistQuestion(q)`（完全重复）
6. INSERT → `SELECT MAX(lwmquestionid)` 获取新ID
7. 如果 `kpids` 参数有值 → `lwmKnowledgePointDAO.saveQuestionKPs(newId, kpIds)`
8. alert结果 → redirect `lwmQueryQuestion`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmAddQuestion.java`

**怎么实现的**：
```java
// 正确答案拼接 — 处理多选
String[] answers = request.getParameterValues("lwmcorrectanswer");
String answer = answers != null ? String.join(",", answers) : "";

// 获取新插入试题的ID（通过MAX查询）
ResultSet rs = db.doQuery(
    "SELECT MAX(lwmquestionid) FROM lwmexamquestion WHERE lwmsubjectid=? AND lwmquestiontype=? AND lwmquestioncontent=?",
    new Object[]{q.getLwmsubjectid(), q.getLwmquestiontype(), q.getLwmquestioncontent()});
```

---

### 55. lwmDeleteQuestion.java — `/lwmDeleteQuestion`

#### 用途
删除试题（含安全检查）。删除前查所有引用此试题的试卷，有引用则列出试卷名并阻止删除。

#### 流程
1. 读 `id` 参数
2. **安全检查**：`lwmpaperDAO.getPapersByQuestionId(id)` → 查询 `lwmpaperquestion` 关联表
3. 如果结果非空 → 拼接试卷名列表 → `alert('该试题已被以下试卷引用，无法删除：\n- 试卷A\n- 试卷B\n请先从对应试卷中移除该试题后再删除。')`
4. 如果为空 → `lwmquestionDAO.lwmDeleteQuestion(id)` → alert成功

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmDeleteQuestion.java`

**怎么实现的**：
```java
List<lwmExamPaper> papers = paperDao.getPapersByQuestionId(id);
if (papers != null && !papers.isEmpty()) {
    StringBuilder msg = new StringBuilder("该试题已被以下试卷引用，无法删除：\\n");
    for (lwmExamPaper p : papers) {
        msg.append("- ").append(p.getLwmpapername()).append("\\n");
    }
    msg.append("\\n请先从对应试卷中移除该试题后再删除。");
    out.println("<script>alert('" + msg.toString() + "');history.back();</script>");
    return;
}
```

**设计要点**：仿照 `lwmDeletePaper` 的 `hasSubmitRecord()` 保护模式，保持代码风格一致性。

---

### 56. lwmLoadQuestions.java — `/lwmLoadQuestions`（AJAX接口）

#### 用途
创建/编辑试卷时的AJAX接口，查询可用试题并以HTML片段返回，供前端直接插入DOM。支持 `exclude` 参数排除已选试题。

#### 流程
1. 验证teacher登录
2. 读 `subject` + `type` + `exclude`（逗号分隔的已选ID列表）
3. `lwmquestionDAO.lwmQueryBySubjectType(subjectId, type, null)` 查全部匹配试题
4. 解析 `exclude` 为 `Set<Integer>` → `stream().filter(q -> !excludeSet.contains(q.getId()))` 过滤
5. 遍历剩余试题 → 输出HTML片段：`<div class="q-item"><input type="checkbox" name="addQuestionIds" value="ID"> [题型标签] 题目内容...</div>`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmLoadQuestions.java`

**怎么实现的**：
```java
// 过滤已选试题
Set<Integer> excludeSet = new HashSet<>();
if (exclude != null && !exclude.isEmpty()) {
    for (String s : exclude.split(","))
        excludeSet.add(Integer.parseInt(s.trim()));
}
List<lwmExamQuestion> filtered = allQuestions.stream()
    .filter(q -> !excludeSet.contains(q.getLwmquestionid()))
    .collect(Collectors.toList());

// 输出HTML片段
for (lwmExamQuestion q : filtered) {
    out.println("<div class='q-item'>");
    out.println("<input type='checkbox' name='addQuestionIds' value='" + q.getLwmquestionid() + "'>");
    out.println("<span class='q-type-badge'>" + q.getLwmquestiontype() + "</span>");
    out.println(escapeHtml(q.getLwmquestioncontent()));  // HTML转义防XSS
    out.println("</div>");
}
```

---

## 4.7 教师-试卷管理（7个Servlet）

---

### 57. lwmCreatePaper.java — `/lwmCreatePaper`

#### 用途
创建试卷。支持手动组卷和自动随机组卷两种模式，包含完整的参数验证（时间、时长、分值）和重复检查。

#### 流程
1. 验证teacher登录
2. 验证参数：
   - `startTime.compareTo(endTime) >= 0` → 拒绝（开始必须早于结束）
   - `examTime <= 0` → 拒绝
   - 各题型分值 `<= 0` → 拒绝
3. **手动模式**（mode=manual）：
   - 读 `questionIds[]` 数组 → 逐题 `qDao.lwmQueryById()` → 加入 `selectedQuestions`
4. **自动模式**（非manual）：
   - 读各题型数量 `danxnum/duoxnum/pdnum/jdnum` → 验证题库数量是否足够 → `qDao.lwmRandomPick()` ×4次
5. 遍历 `selectedQuestions` → 按题型分类 → 统计数量 → 构建题号字符串（如 `"1,3,5,7"`）
6. 构建 `lwmExamPaper` 实体 → 计算总分 = Σ(各题型数量 × 每题分值)
7. `pDao.lwmExistPaperByNameSubject()` 重名检查 → `pDao.lwmAddPaper()` → 循环 `pDao.lwmAddPaperQuestion()`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmCreatePaper.java`

**怎么实现的**：
```java
// 第44-49行 — 时间顺序验证
if (startTime.compareTo(endTime) >= 0) {
    out.println("<script>alert('考试开始时间必须早于结束时间');history.go(-1);</script>");
    return;
}

// 第51-54行 — 考试时长验证
if (examTime <= 0) {
    out.println("<script>alert('考试时间必须大于0分钟');history.go(-1);</script>");
    return;
}

// 第89-98行 — 自动模式：验证题型数量是否足够
Object[][] typeChecks = {{danxNum, "单选题"}, {duoxNum, "多选题"}, {pdNum, "判断题"}, {jdNum, "简答题"}};
for (Object[] tc : typeChecks) {
    int need = (int) tc[0];
    if (need > 0) {
        int available = qDao.lwmCountByType(subjectId, (String) tc[1]);
        if (need > available) {
            // alert 题库不足
            return;
        }
    }
}

// 第106-108行 — 分值必须大于0（不只是非负）
if (danxScore <= 0 || duoxScore <= 0 || pdScore <= 0 || jdScore <= 0) { ... return; }

// 第115-121行 — 遍历试题按题型分类并记录题号
for (int i = 0; i < selectedQuestions.size(); i++) {
    String type = selectedQuestions.get(i).getLwmquestiontype();
    if ("单选题".equals(type)) { danxNum++; danxNos.append(i+1).append(","); }
    // ...其他题型同理
}

// 第135行 — 总分计算
paper.setLwmexamsore(danxNum*danxScore + duoxNum*duoxScore + pdNum*pdScore + jdNum*jdScore);
```

---

### 59. lwmUpdatePaper.java — `/lwmUpdatePaper`

#### 用途
修改试卷。doGet加载试卷+已有试题+候选试题forward编辑页；doPost根据 `hasSubmit` 分两条路径处理。

#### 流程
**doGet**：
1. 查试卷 → 验证所有权（paper.teacherId == teacher.id）
2. `pDao.lwmGetPaperQuestionIds()` → 逐题查 `qDao.lwmQueryById()` → 构建 `currentQuestions`
3. `qDao.lwmQueryBySubjectType()` 查该科目全部试题 → `subjectQuestions`
4. `pDao.hasSubmitRecord(paperId)` 检查提交状态
5. setAttribute → forward `lwmteacher_paper_edit.jsp`

**doPost**：
1. 读取 `lwmpaperid`
2. 检查 `hasSubmit`：
   - **有提交**：只更新基本信息（名称、时间、时长、总分），不改试题组成
   - **无提交**：删旧关联 `lwmDeletePaperQuestions` → 重建试题关联 `lwmAddPaperQuestion` ×N → 重新统计题型 → `lwmUpdatePaperFull`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmUpdatePaper.java`

**怎么实现的（保护逻辑）**：
```java
boolean hasSubmit = pDao.hasSubmitRecord(paperId);
if (!hasSubmit && questionIds != null && questionIds.length > 0) {
    // 可以修改试题组成：先删旧关联，再插入新关联
    pDao.lwmDeletePaperQuestions(paperId);
    for (String qid : questionIds) {
        pDao.lwmAddPaperQuestion(paperId, Integer.parseInt(qid));
    }
    // 重新统计各题型
    pDao.lwmUpdatePaperFull(paper);  // 全字段更新
} else {
    // 有学生提交或没改试题：只更新基本信息
    pDao.lwmUpdatePaper(paper);  // 基本字段更新
}
```

---

### 61. lwmPublishPaper.java — `/lwmPublishPaper`

#### 用途
试卷发布管理。doGet加载发布页面（按科目过滤班级 + 标记已提交班级）；doPost处理发布（排课关系验证 + 提交班级取消保护）。

#### 流程

**doGet**（第30-97行）：
1. 验证teacher → 加载paper → 验证所有权
2. 查排课表：`SELECT DISTINCT lwmclassname FROM lwmstudentcourseteacher WHERE lwmteacherid=? AND lwmsubjectid=?` — **按科目过滤班级**
3. 解析 `paper.lwmclassname` → `publishedClasses` Set
4. 对每个已发布班级查 `lwmexamrecord JOIN lwmstudent` — 有提交记录 → 加入 `submittedClasses`
5. setAttribute → forward `lwmteacher_paper_publish.jsp`

**doPost**（第101-241行）：
1. 读取 `paperId` + `classes[]`
2. 查试卷的 `lwmsubjectid`
3. 对每个选中班级验证排课关系：`SELECT COUNT(*) FROM lwmstudentcourseteacher WHERE teacherid=? AND subjectid=? AND classname=?` → 有效的加入 `validClasses`，无效的加入 `skippedClasses`
4. 计算 `removedSet = 旧发布集合 - 新选中集合`
5. 对每个被移除的班级查提交记录 → 有提交则拒绝 `"班级XXX已有学生完成考试，无法取消发布"`
6. UPDATE `lwmclassname = String.join(",", validClasses)`
7. 提示结果（如有跳过的班级在提示中列出）

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmPublishPaper.java`

**怎么实现的**：
```java
// doGet第59-62行 — 按教师+科目过滤班级（关键数据权限控制）
"SELECT DISTINCT lwmclassname FROM lwmstudentcourseteacher " +
"WHERE lwmteacherid = ? AND lwmsubjectid = ? ORDER BY lwmclassname"

// doGet第77-80行 — 检查班级是否有学生提交
"SELECT COUNT(*) FROM lwmexamrecord r " +
"JOIN lwmstudent s ON r.lwmstudentid = s.lwmstudentid " +
"WHERE r.lwmpaperid = ? AND s.lwmclassname = ? AND r.lwmsubmitstatus IN (1, 2)"

// doPost第137-139行 — 排课关系验证
"SELECT COUNT(*) FROM lwmstudentcourseteacher " +
"WHERE lwmteacherid = ? AND lwmsubjectid = ? AND lwmclassname = ?"

// doPost第177-186行 — 计算被取消的班级（集合差运算）
Set<String> oldSet = new HashSet<>(Arrays.asList(currentClassname.split(",")));
Set<String> newSet = new HashSet<>(Arrays.asList(selectedClasses));
Set<String> removedSet = new HashSet<>(oldSet);
removedSet.removeAll(newSet);  // oldSet - newSet = 被移除的班级
```

---

### 60. lwmDeletePaper.java — `/lwmDeletePaper`

#### 用途
删除试卷（含提交保护）。`hasSubmitRecord()` 为true时阻止删除。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmDeletePaper.java`
```java
if (dao.hasSubmitRecord(paperId)) {
    out.println("<script>alert('该试卷已有学生提交，不可删除');history.go(-1);</script>");
    return;
}
dao.lwmDeletePaper(paperId); // 先删lwmpaperquestion关联，再删lwmexampaper
```

---

## 4.8 教师-知识点管理（1个Servlet）

---

### 64. lwmManageKnowledgePoint.java — `/lwmManageKnowledgePoint`（JSON API）

#### 用途
知识点CRUD的JSON REST API。doGet返回科目下的知识点JSON数组；doPost根据 `action` 参数分流到add/saveQuestionKPs/delete。

#### 流程
**doGet**：读 `subjectid` → `kpDao.queryBySubject()` → 手动拼接JSON字符串 → 输出 `application/json`

**doPost**：
- `action=add`：读 `subjectid+kpname+kpdesc` → `kpDao.insert()` → 查MAX(id)取新ID → 返回 `{success:true, kpid:..., kpname:...}`
- `action=saveQuestionKPs`：读 `questionid+kpids[]` → `kpDao.saveQuestionKPs()` → 返回 `{success:true}`
- `action=delete`：读 `kpid` → `kpDao.countQuestionsByKP()` → >0则返回 `{success:false, message:"该知识点已被试题使用，无法删除"}` → =0则 `kpDao.delete()`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmManageKnowledgePoint.java`

**怎么实现的**：
```java
// doGet返回 — 手动拼JSON（未用JSON库）
response.setContentType("application/json;charset=UTF-8");
StringBuilder json = new StringBuilder("[");
for (int i = 0; i < kpList.size(); i++) {
    lwmKnowledgePoint kp = kpList.get(i);
    json.append("{\"kpid\":").append(kp.getLwmkpid())
        .append(",\"kpname\":\"").append(kp.getLwmkpname())
        .append("\",\"kpdesc\":\"").append(kp.getLwmkpdesc() != null ? kp.getLwmkpdesc() : "")
        .append("\"}");
    if (i < kpList.size() - 1) json.append(",");
}
json.append("]");
out.print(json.toString());

// doPost delete — 安全检查
if ("delete".equals(action)) {
    int kpId = Integer.parseInt(request.getParameter("kpid"));
    int refCount = kpDao.countQuestionsByKP(kpId);
    if (refCount > 0) {
        out.print("{\"success\":false,\"message\":\"该知识点已被试题使用，无法删除\"}");
        return;
    }
    int res = kpDao.delete(kpId);
    out.print("{\"success\":" + (res > 0) + "}");
}
```

---

## 4.9 教师-阅卷（3个Servlet）

---

### 68. lwmGradeExam.java — `/lwmGradeExam`

#### 用途
阅卷入口。加载学生答卷 → 按题型排序 → 如果所有答案分数=0则自动评分客观题 → forward阅卷页。

#### 流程
1. 查 `lwmsubmitstatus`：status=2→拒绝（"成绩已提交，不可修改"）
2. `lwmscoreDAO.lwmQueryAnswersByRecord()` 加载全部答案（JOIN试题表）
3. 按题型 `Comparator` 排序
4. 检查是否所有答案分数==0 → `needAutoScore=true`
5. 加载试卷配置（获取各题型满分）
6. 自动评分：遍历答案 → 单选/判断 trim比对 → 多选 排序后数组比对 → 简答 跳过
7. setAttribute → forward `lwmteacher_grading.jsp`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmGradeExam.java`

**怎么实现的（自动评分逻辑）**：
```java
// 单选题和判断题：直接比对
if ("单选题".equals(type) || "判断题".equals(type)) {
    return studentAns.trim().equals(correctAns.trim()) ? maxScore : 0;
}
// 多选题：拆分→排序→数组比对（"A,C" 和 "C,A" 视为相同）
if ("多选题".equals(type)) {
    String[] sArr = splitAnswer(studentAns);
    String[] cArr = splitAnswer(correctAns);
    Arrays.sort(sArr);
    Arrays.sort(cArr);
    return Arrays.equals(sArr, cArr) ? maxScore : 0;
}
// 简答题：不自动评分
return 0;
```

---

### 69. lwmSubmitScore.java — `/lwmSubmitScore`

#### 用途
保存教师评分结果。遍历所有得分输入 → 验证范围 → 逐题保存 → 累加总分 → 记录错题 → 可选finalize。

#### 流程
1. 加载试卷配置获取各题型满分
2. JOIN查询 `lwmstudentanswer + lwmexamquestion` 获取每个answerId对应的题型（用于分数范围验证）
3. 遍历 `score_<answerId>` 参数：
   - 根据题型获取maxScore
   - `if (score < 0 || score > maxScore)` → `alert('分数无效')` + return
   - `lwmscoreDAO.lwmSaveQuestionScore(answerId, score)`
   - `totalScore += score`
4. `lwmscoreDAO.lwmSaveScore(examScore)` INSERT总分
5. **自动记录错题**：遍历所有答案 → 得分<满分的 → `lwmMistakeBookDAO.upsertMistake(studentId, questionId, true)`
6. 如果 `finalize=true` → UPDATE `lwmsubmitstatus=2`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmSubmitScore.java`

**怎么实现的（核心三个步骤）**：
```java
// 第68-86行 — 分数验证+保存
while (paramNames.hasMoreElements()) {
    String name = paramNames.nextElement();
    if (name.startsWith("score_")) {
        int answerId = Integer.parseInt(name.substring(6));
        int score = Integer.parseInt(request.getParameter(name));
        // 根据题型获取满分上限
        String type = answerTypes.get(answerId);
        int maxScore = (type对应paper中的该题型分值);
        if (score < 0 || (maxScore > 0 && score > maxScore)) {
            out.println("<script>alert('分数无效，应在0到" + maxScore + "分之间');</script>");
            return;
        }
        dao.lwmSaveQuestionScore(answerId, score);
        totalScore += score;
    }
}

// 第98行 — 记录错题
recordMistakes(studentId, paperId, recordId, paper);

// 第119-143行 — recordMistakes方法实现
private void recordMistakes(...) {
    // JOIN查询答案+题型 → 判断 score < maxScore → upsertMistake()
    boolean isWrong = score < maxScore;
    mbDao.upsertMistake(studentId, questionId, isWrong);
}
```

---

## 4.10 教师-成绩分析（3个Servlet）

---

### 71. lwmScoreAnalysisAction.java — `/lwmScoreAnalysis`

#### 用途
成绩分析仪表盘。单试卷模式：计算统计指标+5段分布+及格率+排名；比较模式(compare)：多班级JSON对比。

#### 流程
1. 加载科目、试卷、班级三个下拉框数据 → 从 `lwmexampaper` 表 split 逗号分隔班级名
2. 读取试卷的 `lwmexamsore`（总分）
3. **动态计算阈值**：`passLine = (int)(totalScore * 0.6)`、`excelLine = (int)(totalScore * 0.9)`、区间边界 = 70%/80%/90%
4. 查询统计数据：AVG/MAX/MIN/STDDEV
5. 查询5段分布：每段人数
6. 查询及格率 + 学生排名
7. forward `lwmteacher_score_analysis.jsp`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmScoreAnalysisAction.java`

**怎么实现的（动态及格线）**：
```java
// 核心：从试卷总分动态计算所有阈值
int totalScore = paper.getLwmexamsore();
int passLine  = (int)(totalScore * 0.6);    // 及格线 = 总分×60%
int b2End     = (int)(totalScore * 0.7);    // 及格-70%
int b3End     = (int)(totalScore * 0.8);    // 70%-80%
int excelLine = (int)(totalScore * 0.9);    // 优秀线 = 总分×90%

// SQL中所有硬编码60和90被替换为Java变量
// 例如：分布查询的CASE WHEN score BETWEEN 0 AND " + (passLine-1)
//      及格率查询的 CASE WHEN score >= " + passLine
```

**设计演进**：之前硬编码60分及格线，不适用于150分试卷。改为百分比动态计算后适用于任何满分。

---

### 72. lwmQuestionQualityAction.java — `/lwmQuestionQuality`（JSON API）

#### 用途
试题质量分析。计算每道题的难度指数和区分度，返回JSON供JSP渲染。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmQuestionQualityAction.java`

**怎么实现的**：
```java
// 区分度 = 高分组正确率 - 低分组正确率（经典测量理论）
// 高分组 = 总分前27%的学生，低分组 = 总分后27%的学生
double topRate = (double) topCorrect / topStudentCount;    // 高分组正确率
double bottomRate = (double) bottomCorrect / bottomStudentCount; // 低分组正确率
double discrimination = topRate - bottomRate;               // 区分度

// 难度 = 正确人数 / 总答题人数（越高越容易）
double difficulty = (double) correctCount / totalStudents;
// 映射为1-4星评级
```

---

### 73. lwmKnowledgeAnalysisAction.java — `/lwmKnowledgeAnalysis`（JSON API）

#### 用途
知识点掌握度分析。总体模式：每个知识点的得分率；比较模式：每个班级在每个知识点上的得分率。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmKnowledgeAnalysisAction.java`

**怎么实现的**：
```java
// 聚合查询 — 按知识点分组统计得分率
"SELECT kp.lwmkpid, kp.lwmkpname, " +
"COUNT(DISTINCT sa.lwmquestionid) AS q_count, " +
"AVG(CASE WHEN sa.lwmquestionscore > 0 THEN 1 ELSE 0 END) AS score_rate " +  // 得分率
"FROM ... WHERE ... GROUP BY kp.lwmkpid, kp.lwmkpname " +
"ORDER BY score_rate ASC"  // 最薄弱的知识点排最前
```

---

## 4.11 学生-考试流程（5个Servlet）— 核心链路

---

### 74. lwmTakeExam.java — `/lwmTakeExam`

#### 用途
学生进入考试的完整门禁控制。验证班级权限（FIND_IN_SET逻辑）、时间窗口（start≤now≤end）、首次进入创建记录（记录开始时间）、再次进入恢复草稿、加载试题按题型排序。

#### 流程
1. 验证student登录
2. 加载paper → 遍历 `paper.lwmclassname.split(",")` 比对 `student.lwmclassname` → 不匹配则拒绝
3. **时间窗口校验**：多种格式解析 `startTime`/`endTime` → `now.isBefore(startTime)` → "考试还未开始"；`now.isAfter(endTime)` → "考试已结束"
4. 加载所有试题 → 按题型Comparator排序（单选→多选→判断→简答）
5. **草稿检测**：`SELECT FROM lwmexamrecord WHERE paperid=? AND studentid=? AND status=0`
   - 存在 → 读取 `recordId` + `recordStartTime` + 已保存答案到 `draftAnswers Map`
   - 不存在 → INSERT新记录（`status=0, starttime=NOW()`） → 获取自增ID
6. 设置5个request属性 → forward `lwmstudent_take_exam.jsp`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmTakeExam.java`（第1-162行）

**怎么实现的**：
```java
// 第38-48行 — 班级权限验证（拆分逗号分隔的班级列表）
boolean classMatch = false;
if (paper != null && paper.getLwmclassname() != null) {
    for (String cls : paper.getLwmclassname().split(",")) {
        if (cls.trim().equals(student.getLwmclassname())) {
            classMatch = true;
            break;
        }
    }
}

// 第51-69行 — 时间窗口校验（支持多种时间格式）
String[] patterns = {"yyyy-MM-dd HH:mm:ss", "yyyy-MM-dd HH:mm:ss.S",
                     "yyyy-MM-dd'T'HH:mm", "yyyy-MM-dd HH:mm"};
LocalDateTime now = LocalDateTime.now();
if (now.isBefore(startTime)) { alert("考试还未开始"); return; }
if (now.isAfter(endTime))   { alert("考试已结束");   return; }

// 第115-131行 — 首次进入创建记录（记录开始时间用于倒计时）
recordStartTime = new java.sql.Timestamp(System.currentTimeMillis());
PreparedStatement ips = conn.prepareStatement(
    "INSERT INTO lwmexamrecord(lwmpaperid,lwmstudentid,lwmstarttime,lwmendtime,lwmsubmitstatus) " +
    "VALUES(?,?,?,?,0)", Statement.RETURN_GENERATED_KEYS);
ips.setTimestamp(3, recordStartTime);   // 记录首次进入的精确时间
ips.executeUpdate();
ResultSet keys = ips.getGeneratedKeys();
if (keys.next()) draftRecordId = keys.getInt(1);  // 获取自增ID
```

**关键设计**：
- 再次进入时复用已有 `lwmstarttime`，不重置。这意味着刷新页面倒计时不会重新开始。
- 使用 `RETURN_GENERATED_KEYS` 获取自增ID（比SELECT MAX更可靠）。
- `parseTime()` 方法支持4种datetime格式，兼容数据库格式和HTML datetime-local格式。

---

### 75. lwmSaveExamDraft.java — `/lwmSaveExamDraft`

#### 用途
考试中途保存草稿。查找或创建草稿记录(status=0)，删除旧答案后插入新答案。

#### 流程
1. 查草稿记录：`SELECT FROM lwmexamrecord WHERE paperid=? AND studentid=? AND status=0`
2. 有草稿 → 获取 `recordId` → `DELETE FROM lwmstudentanswer WHERE lwmrecordid=?`
3. 无草稿 → INSERT新记录(status=0) → SELECT MAX获取ID
4. 遍历请求参数中 `q_` 开头的参数 → `INSERT INTO lwmstudentanswer`
5. 剩余未答的题也插入空答案

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmSaveExamDraft.java`

---

### 76. lwmSubmitExam.java — `/lwmSubmitExam`

#### 用途
最终提交考试。包含超时检查、答案保存、状态更新。支持手动提交和倒计时归零自动提交。

#### 流程
1. 查草稿记录 → 获取 `recordStartTime`
2. **超时校验**：`deadline = recordStartTime + examTime * 60 * 1000ms`
   - 自动提交（`autoSubmit=true`）：容忍5秒（`tolerance=5000`）
   - 手动提交：容忍0秒（`tolerance=0`）
   - 超时 → `alert('考试时间已到，无法提交')` + return
3. UPDATE记录 `status=1` +记录 `endtime`
4. `DELETE FROM lwmstudentanswer WHERE lwmrecordid=?` 清除旧草稿答案
5. 遍历 `q_` 参数 → INSERT最终答案
6. 未答的题也插入空答案（score=0）
7. 自动提交 → `alert('考试时间到，系统已自动交卷')` + `parent.location.href`
8. 手动提交 → `alert('交卷成功！等待教师批阅。')` + `parent.location.href`

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmSubmitExam.java`（第1-132行）

**怎么实现的**：
```java
// 第50-57行 — 服务端二次超时校验
if (recordStartTime != null && paper != null && paper.getLwmexamtime() > 0) {
    long deadline = recordStartTime.getTime() + paper.getLwmexamtime() * 60 * 1000L;
    long tolerance = isAutoSubmit ? 5000 : 0;  // 自动提交给5秒宽限期
    if (System.currentTimeMillis() > deadline + tolerance) {
        out.println("<script>alert('考试时间已到，无法提交');history.back();</script>");
        return;
    }
}

// 第97-101行 — 用parent.location.href避免frameset嵌套
if (isAutoSubmit) {
    out.println("<script>alert('考试时间到，系统已自动交卷');parent.location.href='lwmstudent_main.jsp';</script>");
} else {
    out.println("<script>alert('交卷成功！等待教师批阅。');parent.location.href='lwmstudent_main.jsp';</script>");
}

// 第104-131行 — saveAnswers方法（未答的题也插空答案）
for (int qid : allQuestionIds) {
    if (!savedIds.contains(qid)) {
        db2.doUpdate("INSERT INTO lwmstudentanswer(...) VALUES(?,?,?,0,?,?)",
            new Object[]{recordId, qid, "", studentId, paperId});
    }
}
```

**关键设计**：
- `parent.location.href` 而非 `location.href`：因为考试页面在frameset的rightFrame中，需要用parent跳出嵌套。
- 未答的题也插入空答案记录：确保所有题目在阅卷时都有记录可查。
- 自动提交5秒宽限期：因为JS setTimeout可能略有延迟。

---

### 77. lwmViewExam.java — `/lwmViewExam`

#### 用途
学生查看已提交的试卷详情。仅 `status != 0` 的记录可查看。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmViewExam.java`

**怎么实现的**：
```java
// 设置每题的满分值（从试卷配置中按题型匹配）
if (answers != null && paperId > 0) {
    lwmExamPaper paper = pDao.lwmQueryPaperById(paperId);
    for (lwmStudentAnswer ans : answers) {
        String type = ans.getLwmquestiontype();
        if ("单选题".equals(type)) ans.setLwmpaperscore(paper.getLwmdanxscore());
        else if ("多选题".equals(type)) ans.setLwmpaperscore(paper.getLwmduoxscore());
        // ...
    }
}
```

---

### 78. lwmUpdateStudentAnswer.java — `/lwmUpdateStudentAnswer`

#### 用途
学生在已提交但未评分（status=1）的状态下修改答案。修改后得分重置为0。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmUpdateStudentAnswer.java`
```java
// 仅status=1可修改
if (status != 1) { alert("不能修改答案"); return; }
// 遍历ans_参数 → UPDATE答案 + 重置score=0
```

---

## 4.12 学生-错题本（2个Servlet）

### 79. lwmMistakeBookAction.java — `/lwmMistakeBook`

#### 用途
学生错题本。doGet：三级筛选（科目+知识点+复习状态）+分页；doPost：更新复习状态。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmMistakeBookAction.java`
**怎么实现的**：Fpage分页（每页10条），doPost读 `action=updateStatus` + `questionId` + `status` → `mbDao.updateReviewStatus()`

### 80. lwmKnowledgeMasteryAction.java — `/lwmKnowledgeMastery`（JSON API）

#### 用途
学生端知识点掌握率雷达图数据API。调用 `getKPMastery()` 返回JSON。

#### 核心代码
**在哪里**：`src/main/java/com/example/lwmexam/action/lwmexam/lwmKnowledgeMasteryAction.java`
```java
// mastery = 1 - wrong/total（掌握率），最低为0
double mastery = Math.max(0, 1.0 - wrongCount / (double) totalQuestions);
```

---

## 4.13 其余Servlet简要列表

| 编号 | 文件 | 用途 |
|------|------|------|
| 34-39 | lwmAddteacher等 | 教师CRUD（结构同学生CRUD） |
| 40-45 | lwmAddsubject等 | 课程CRUD |
| 46-51 | lwmAddsourse等 | 排课CRUD（含排重检查） |
| 65 | lwmQueryTeacherCourses | 教师查看自己的排课 |
| 66 | lwmViewClassStudents | 查看某班级学生名单 |
| 67 | lwmQueryExamRecords | 教师考试记录列表（四表JOIN） |
| 70 | lwmDeleteExamRecord | 删除考试记录（验证所有权+级联删除） |
| 81 | HelloServlet | 测试Servlet |

---

# 第五部分：视图层 — 45个JSP

## 5.1 通用页面（3个）

---

### 82. login.jsp — 登录页面

#### 用途
系统入口页面（web.xml的welcome-file）。毛玻璃卡片设计，表单POST到 `lwmLogin`。

#### 流程
用户输入账号+密码+选角色 → JS `validateForm()` 非空校验 → 表单POST `lwmLogin`

#### 核心代码
**在哪里**：`src/main/webapp/login.jsp`

**怎么实现的**：
```html
<!-- 表单 -->
<form action="lwmLogin" method="post" onsubmit="return validateForm()">
    <input type="text" name="account" placeholder="请输入账号" required>
    <input type="password" name="password" placeholder="请输入密码" required>
    <input type="radio" name="role" value="admin" checked> 管理员
    <input type="radio" name="role" value="teacher"> 教师
    <input type="radio" name="role" value="student"> 学生
    <button type="submit">登录</button>
</form>

<script>
function validateForm() {
    // 第~80行 — 简单的非空校验
    if (account === '' || password === '') { alert('请填写完整信息'); return false; }
    return true;
}
</script>
```
**样式**：全屏背景图 `url("xy.jpg")` + `rgba(0,0,0,0.3)` 半透明遮罩 + `backdrop-filter: blur(10px)` 毛玻璃效果。

---

### 83. lwmfoot.jsp — 分页组件

#### 用途
可复用的分页导航栏片段，被所有列表页面 `<jsp:include>` 引入。

#### 流程
从request读取 `${fp}`（Fpage实例） → 计算当前页码+总页数 → 渲染4个导航按钮 + 状态文字

#### 核心代码
**在哪里**：`src/main/webapp/lwmfoot.jsp`

**怎么实现的**：
```jsp
<!-- 状态文字 -->
<div class="message">
    共 ${fp.rowCount} 条记录，共 ${fp.pageCount} 页，当前显示第 ${fp.pageNow + 1} 页
</div>

<!-- 首页按钮 -->
<c:choose>
    <c:when test="${fp.pageNow != 0}">
        <a href="${pageUrl}?page=0&tj=${tj}">|&lt;</a>
    </c:when>
    <c:otherwise>
        <a href="javascript:void(0);" class="disabled">|&lt;</a>
    </c:otherwise>
</c:choose>
<!-- 上一页、下一页、末页同理 -->
```
**关键**：`pageNow` 是0-based（Java层），显示时 `+1` 转换为1-based。`tj` 参数保持筛选条件跨页传递。

---

## 5.2 管理员端JSP（16个）

---

### 85-88. 框架四件套

- **lwmadmin_main.jsp**：`<frameset rows="88,*">` → `<frame src="lwmadmin_top.jsp">` → `<frameset cols="187,*">` → `<frame src="lwmadmin_left.jsp">` + `<frame src="lwmadmin_index.jsp">`
- **lwmadmin_top.jsp**：顶部栏，显示管理员姓名 `${admin.lwmadminname}`，注销链接 `href="SystemExit" target="_parent"`
- **lwmadmin_left.jsp**：侧栏，5个菜单链接(target="rightFrame")，JS实现点击高亮
- **lwmadmin_index.jsp**：仪表盘，内嵌Java查询4项统计(COUNT)，4张统计卡片+考试安排占位表格

### 89-100. CRUD页面（12个）

**列表页4个**（lwmstudentlist、lwmteacherlist、lwmsubjectlist、lwmcourselist）：
统一结构 — 搜索框+添加按钮+表格(每行修改/删除按钮)+Excel区+`<jsp:include page="lwmfoot.jsp"/>`

**添加页4个**（addstudent、addteacher、addsubject、addcourse）：
统一结构 — 表单卡片+返回链接+JS validateForm()+各字段输入

**编辑页4个**（updatestudent、updateteacher、updatesubject、updatecourse）：
与添加页同结构，字段用 `${entity.field}` 预填

**关键JSP细节**：

**lwmaddstudent.jsp 专业-班级联动**：
```javascript
// 约第150行 — 选择专业后班级下拉框动态更新
const classMapping = {
    '计算机科学与技术': ['计科1班','计科2班','计科3班','计科4班'],
    '软件工程': ['软工1班','软工2班','软工3班','软工4班'],
    // ...
};
function updateClassOptions() {
    const major = document.getElementById('lwmmajor').value;
    const classes = classMapping[major] || [];
    classSelect.innerHTML = '<option value="">请选择班级</option>';
    classes.forEach(c => { classSelect.innerHTML += '<option value="' + c + '">' + c + '</option>'; });
}
```

---

## 5.3 教师端JSP（18个）

---

### 101-104. 框架四件套
与管理员端结构一致，绿色主题。`lwmteacher_left.jsp` 有6个导航入口。

---

### 106. lwmteacher_question_add.jsp — 试题添加/编辑页（最复杂的教师JSP，~400行）

#### 用途
试题添加和编辑共用的表单页。根据 `question` 属性是否为null判断模式。

#### 流程
1. 查询教师排课 → 填充科目下拉框（只显示教师教的科目）
2. 编辑模式：加载已有试题数据 + 已关联的知识点
3. 题型切换：`onchange="toggleOptions()"` 动态显示/隐藏选项区
4. 知识点区域：AJAX加载checkbox列表 → 快速添加 → ×删除
5. 表单提交前 `validateForm()` → 验证必填项 → POST到 `lwmAddQuestion` 或 `lwmUpdateQuestion`

#### 核心代码
**在哪里**：`src/main/webapp/lwmteacher_question_add.jsp`

**怎么实现的**：
```javascript
// toggleOptions() — 根据题型动态切换UI（约第200行）
function toggleOptions() {
    const type = document.getElementById('lwmquestiontype').value;
    // 单选题：显示4个radio + 隐藏判断区 + 显示选项区
    if (type === '单选题') {
        optionsArea.style.display = 'block';
        judgeAnswerArea.style.display = 'none';
        textAnswerArea.style.display = 'none';
        // 将答案区转换为radio
        answerDiv.innerHTML = '<input type="radio" name="lwmcorrectanswer" value="A">A ...';
    }
    // 多选题：显示4个checkbox
    else if (type === '多选题') {
        answerDiv.innerHTML = '<input type="checkbox" name="lwmcorrectanswer" value="A">A ...';
    }
    // 判断题："对"/"错"两个radio
    else if (type === '判断题') {
        optionsArea.style.display = 'none';
        judgeAnswerArea.style.display = 'block';
    }
    // 简答题：文本框
    else if (type === '简答题') {
        optionsArea.style.display = 'none';
        textAnswerArea.style.display = 'block';
    }
}

// loadKnowledgePoints() — AJAX加载知识点（约第150行）
function loadKnowledgePoints() {
    fetch('lwmManageKnowledgePoint?subjectid=' + subjectId)
        .then(r => r.json())
        .then(data => {
            // 渲染checkbox列表
            data.forEach(kp => {
                html += '<label><input type="checkbox" name="kpids" value="' + kp.kpid + '">' + kp.kpname + '</label>';
            });
            kpContainer.innerHTML = html;
        });
}

// deleteKP() — AJAX删除知识点（约第250行）
function deleteKP(kpId) {
    if (!confirm('确定删除该知识点？删除后不可恢复')) return;
    fetch('lwmManageKnowledgePoint', {
        method: 'POST',
        headers: {'Content-Type': 'application/x-www-form-urlencoded'},
        body: 'action=delete&kpid=' + kpId
    }).then(r => r.json()).then(data => {
        if (data.success) { /* 从列表移除 */ }
        else { alert(data.message); }
    });
}
```

---

### 108. lwmteacher_paper_create.jsp — 创建试卷页（~300行）

#### 用途
创建试卷，支持手动组卷和自动随机组卷两个Tab切换。

#### 核心代码
**在哪里**：`src/main/webapp/lwmteacher_paper_create.jsp`

**怎么实现的**：
```javascript
// switchMode() — 手动/自动模式切换（约第100行）
function switchMode(mode) {
    document.getElementById('mode').value = mode;
    if (mode === 'manual') {
        manualArea.style.display = 'block';
        autoArea.style.display = 'none';
        manualTab.classList.add('active');
        autoTab.classList.remove('active');
    } else {
        manualArea.style.display = 'none';
        autoArea.style.display = 'block';
    }
}

// loadQuestions() — AJAX加载可选题库（约第150行）
function loadQuestions() {
    const subject = document.getElementById('lwmsubjectid').value;
    const type = document.getElementById('questionTypeFilter').value;
    fetch('lwmLoadQuestions?subject=' + subject + '&type=' + type)
        .then(r => r.text())
        .then(html => {
            document.getElementById('questionList').innerHTML = html;
            updateTotal();
        });
}

// updateTotal() — 实时计算试卷总分（约第200行）
function updateTotal() {
    let total = 0;
    // 手动模式：统计勾选的各题型数量 × 分值
    if (mode === 'manual') {
        const danxCount = questionList.querySelectorAll('.badge-danx').length;
        total += danxCount * parseInt(document.getElementById('danxscore').value);
        // ... 同样计算其他题型
    }
    // 自动模式：各题型数量 × 分值
    else {
        total += parseInt(document.getElementById('danxnum').value) * parseInt(document.getElementById('danxscore').value);
        // ...
    }
    document.getElementById('totalScore').textContent = total;
}
```

---

### 109. lwmteacher_paper_edit.jsp — 编辑试卷页（~500行，系统最复杂JSP）

#### 用途
编辑试卷。有提交保护：已提交则锁定试题组成。支持添加/移除试题、自动抽题。

#### 核心代码
**在哪里**：`src/main/webapp/lwmteacher_paper_edit.jsp`

**怎么实现的（关键JS逻辑）**：
```javascript
// existingIds — 全局Set追踪已选试题ID，防止重复添加
let existingIds = new Set();

// getExcludeParam() — 构建排除列表（约第100行）
function getExcludeParam() {
    return Array.from(existingIds).join(',');
}

// editLoadQuestions() — AJAX加载可选试题（排除已选的）（约第150行）
function editLoadQuestions() {
    fetch('lwmLoadQuestions?subject=' + subjectId + '&type=' + type + '&exclude=' + getExcludeParam())
        .then(r => r.text())
        .then(html => { /* 插入候选区 */ });
}

// addSelectedQuestions() — 将勾选的候选试题移到当前试题列表（约第200行）
function addSelectedQuestions() {
    const checked = candidateList.querySelectorAll('input:checked');
    checked.forEach(cb => {
        const qid = parseInt(cb.value);
        if (!existingIds.has(qid)) {
            existingIds.add(qid);  // 加入全局Set
            // 插入到当前试题列表
            currentList.innerHTML += buildQuestionItem(cb);
        }
    });
    updateTotal();
}

// editAutoPick() — AJAX随机抽题并加入（约第250行）
function editAutoPick() {
    const params = 'subject=' + subjectId + '&exclude=' + getExcludeParam() +
                   '&danxnum=' + danxNum + '&duoxnum=' + duoxNum + ...;
    fetch('lwmRandomPickQuestions?' + params)
        .then(r => r.text())
        .then(html => {
            currentList.innerHTML += html;
            updateTotal();
        });
}

// updateTotal() — 从当前试题列表计算总分
function updateTotal() {
    // 统计各题型数量 × 对应分值
}
```

---

### 117. lwmteacher_score_analysis.jsp — 成绩分析页（~400行）

#### 用途
成绩分析仪表盘，4个Tab（分数概览/试题质量/知识点分析/班级对比）。

#### 核心代码
**在哪里**：`src/main/webapp/lwmteacher_score_analysis.jsp`

**怎么实现的（ECharts集成）**：
```javascript
// 分布图 — 使用动态bracketLabels（来自后端计算）
const distChart = echarts.init(document.getElementById('distChart'));
distChart.setOption({
    xAxis: { data: bracketLabels },  // 如 [0-89, 90-104, 105-119, 120-134, 135-150]
    series: [{ data: distribution, type: 'bar' }]
});

// 试题质量 — AJAX懒加载（Tab2首次点击时加载）
let qualityLoaded = false;
function loadQuestionQuality() {
    if (qualityLoaded) return;
    qualityLoaded = true;
    fetch('lwmQuestionQuality?paperid=' + paperId)
        .then(r => r.json())
        .then(data => {
            // 渲染难度★和区分度表格
        });
}
```

---

### 118. lwmteacher_class_compare.jsp — 班级对比页（~400行）

#### 用途
多班级成绩对比。3个Tab + ECharts多系列图表。

#### 核心代码（已修复的Bug相关）
**在哪里**：`src/main/webapp/lwmteacher_class_compare.jsp`

**怎么实现的**：
```javascript
// 科目切换 — 重建paper下拉框（修复Bug：不能用display:none隐藏option）
function onSubjectChange() {
    paperSelect.innerHTML = '<option value="">请选择试卷</option>';
    allPaperOptions.forEach(opt => {
        if (opt.subjectId == selectedSubjectId) {
            paperSelect.innerHTML += '<option value="' + opt.id + '">' + opt.name + '</option>';
        }
    });
    onPaperChange(); // 级联清空班级选择
}

// 开始对比前 — 重置所有渲染状态（修复Bug：第二次对比不刷新）
function startCompare() {
    coreMetricsRendered = false;
    distRendered = false;
    kpLoaded = false;
    // 销毁旧ECharts实例
    coreChartInst && coreChartInst.dispose();
    distChartInst && distChartInst.dispose();
    if (kpChartInst) kpChartInst.dispose();
    // 清空旧表格
    document.getElementById('coreTable').innerHTML = '';
    // 重新获取数据
    fetch('lwmScoreAnalysis?action=compare&paperid=' + paperId + '&classnames=' + classes)
        .then(r => r.json())
        .then(renderCoreMetrics);
}
```

---

## 5.4 学生端JSP（9个）

---

### 125. lwmstudent_take_exam.jsp — 在线考试页（~500行，整个系统最复杂的JSP）

#### 用途
在线考试主页面。左侧答题卡 + 右侧题目区 + 倒计时 + 草稿保存 + 自动/手动提交。

#### 核心代码
**在哪里**：`src/main/webapp/lwmstudent_take_exam.jsp`

**怎么实现的**：

```javascript
// 1. 倒计时 — 约第50行
let remainingSeconds = /* JSP计算的初始剩余秒数 */;
function updateTimer() {
    if (remainingSeconds <= 0) {
        // 时间到：自动提交
        document.getElementById('autoSubmit').value = 'true';
        document.getElementById('examForm').action = 'lwmSubmitExam';
        document.getElementById('examForm').submit();
        return;
    }
    remainingSeconds--;
    const h = Math.floor(remainingSeconds / 3600);
    const m = Math.floor((remainingSeconds % 3600) / 60);
    const s = remainingSeconds % 60;
    timerEl.textContent = pad(h) + ':' + pad(m) + ':' + pad(s);
    // 少于5分钟 → 红色闪烁警告
    if (remainingSeconds < 300) timerEl.classList.add('timer-warning');
}
setInterval(updateTimer, 1000);

// 2. 答题卡 — 题号按钮网格 + 实时颜色更新（约第200行）
function scrollToQuestion(qid) {
    document.getElementById('question_' + qid).scrollIntoView({behavior: 'smooth', block: 'center'});
}
// 监听所有题目输入变化 → 更新对应按钮颜色
document.getElementById('examForm').addEventListener('change', function(e) {
    // 找到最近的 .card 元素 → 提取data-qid → 更新按钮class
    const card = e.target.closest('.card');
    if (card) updateSidebarButton(card.dataset.qid);
});
// 从draftAnswers初始化按钮颜色
function initSidebar() {
    draftAnswers.forEach((answer, qid) => {
        if (answer && answer.trim() !== '') {
            document.querySelector('.sidebar-btn[data-qid="' + qid + '"]').classList.add('answered');
        }
    });
}

// 3. 未答题提醒 — 约第350行
function countUnanswered() {
    let count = 0;
    document.querySelectorAll('.card').forEach(card => {
        const type = card.dataset.type;
        if (type === '简答题') {
            const ta = card.querySelector('textarea');
            if (!ta || ta.value.trim() === '') count++;
        } else {
            const checked = card.querySelectorAll('input:checked');
            if (checked.length === 0) count++;
        }
    });
    return count;
}
function submitExam() {
    const unanswered = countUnanswered();
    if (unanswered > 0) {
        if (!confirm('还有 ' + unanswered + ' 道题未作答，确定要提交吗？')) return;
    }
    document.getElementById('examForm').action = 'lwmSubmitExam';
    document.getElementById('examForm').submit();
}

// 4. 草稿保存 — 约第400行
function saveDraft() {
    document.getElementById('examForm').action = 'lwmSaveExamDraft';
    document.getElementById('examForm').submit();
}

// 5. 答题卡折叠 — 约第100行
function toggleSidebar() {
    sidebar.classList.toggle('collapsed');  // 宽度 210px ↔ 40px
}
```

**JSP端渲染逻辑**（约第50-150行）：
```jsp
<%
// 遍历questions列表，按题型分组渲染
String currentType = "";
for (lwmExamQuestion q : questions) {
    if (!q.getLwmquestiontype().equals(currentType)) {
        // 新题型 → 输出题型标题
        currentType = q.getLwmquestiontype();
%>
    <div class="section-title"><%= currentType %></div>
<%
    }
%>
    <div class="card" id="question_<%= q.getLwmquestionid() %>" data-qid="<%= q.getLwmquestionid() %>" data-type="<%= q.getLwmquestiontype() %>">
        <!-- 根据题型渲染不同的输入控件 -->
        <% if ("单选题".equals(q.getLwmquestiontype())) { %>
            <input type="radio" name="q_<%= q.getLwmquestionid() %>" value="A"
                   <%= isOptionSelected(draftAnswers, q.getLwmquestionid(), "A") ? "checked" : "" %>> A. <%= q.getLwmoptiona() %>
            <!-- B/C/D同理 -->
        <% } else if ("多选题".equals(q.getLwmquestiontype())) { %>
            <input type="checkbox" name="q_<%= q.getLwmquestionid() %>" value="A">
            <!-- ... -->
        <% } else if ("判断题".equals(q.getLwmquestiontype())) { %>
            <input type="radio" name="q_<%= q.getLwmquestionid() %>" value="对"> 对
            <input type="radio" name="q_<%= q.getLwmquestionid() %>" value="错"> 错
        <% } else { %>
            <textarea name="q_<%= q.getLwmquestionid() %>"><%= draftAnswers.get(q.getLwmquestionid()) != null ? draftAnswers.get(q.getLwmquestionid()) : "" %></textarea>
        <% } %>
    </div>
<%
}
%>
```

**所有输入控件的name格式**：`q_<questionId>`，后端通过 `startsWith("q_")` 遍历收集。

---

### 126. lwmstudent_view_exam.jsp — 查看试卷结果页

#### 用途
学生查看已提交试卷的答案、得分和颜色编码反馈。

#### 核心代码
**在哪里**：`src/main/webapp/lwmstudent_view_exam.jsp`

**怎么实现的**：
```jsp
<%!
// 多选题比对方法 — 拆分→排序→数组比对
String[] splitAnswer(String ans) {
    if (ans == null || ans.isEmpty()) return new String[0];
    if (ans.contains(",")) return ans.split(",");
    if (ans.contains("，")) return ans.split("，");
    // 无逗号：单个字符拆分
    return ans.split("");
}
boolean isMultiSelectCorrect(String studentAns, String correctAns) {
    String[] sArr = splitAnswer(studentAns);
    String[] cArr = splitAnswer(correctAns);
    Arrays.sort(sArr);
    Arrays.sort(cArr);
    return Arrays.equals(sArr, cArr);
}
%>

<!-- 颜色编码边框 -->
<div class="card <%= score == maxScore ? "correct-bg" : (score > 0 ? "partial-bg" : "wrong-bg") %>">
```

---

### 127. lwmstudent_mistakebook.jsp — 错题本页（~350行）

#### 用途
学生错题本和知识点掌握度分析，两个Tab。

#### 核心代码
**在哪里**：`src/main/webapp/lwmstudent_mistakebook.jsp`

**怎么实现的**：
```javascript
// 知识点雷达图 — ECharts配置（约第250行）
function loadRadar() {
    fetch('lwmKnowledgeMastery?subjectid=' + subjectId)
        .then(r => r.json())
        .then(data => {
            const chart = echarts.init(document.getElementById('radarChart'));
            chart.setOption({
                radar: {
                    indicator: data.map(d => ({ name: d.kpname, max: 100 }))
                },
                series: [{
                    type: 'radar',
                    data: [{ value: data.map(d => d.mastery), name: '掌握率' }]
                }]
            });
            // 渲染知识点统计表
            data.forEach(d => {
                const color = d.mastery >= 70 ? 'green' : d.mastery >= 40 ? 'yellow' : 'red';
                // ...
            });
        });
}

// 错题卡片展开/折叠（约第100行）
function toggleDetail(header) {
    header.nextElementSibling.classList.toggle('open');
    header.querySelector('.chevron').classList.toggle('rotated');
}
```

---

### 119-124. 学生端其余JSP

| 文件 | 用途 |
|------|------|
| lwmstudent_main.jsp | frameset框架 |
| lwmstudent_top.jsp | 顶部栏（橙/金渐变，含姓名学号+注销） |
| lwmstudent_left.jsp | 侧栏4菜单（考试中心/我的试卷/错题本/个人信息） |
| lwmstudent_index.jsp | 考试中心（可用考试列表+时间状态判断） |
| lwmstudent_paper.jsp | 考试记录列表（已提交/已评分状态） |
| lwmstudent_message.jsp | 个人信息只读卡片 |

---

# 第六部分：全部文件索引（按编号速查）

| 编号 | 文件名 | 类型 | 用途一句话 |
|------|--------|------|-----------|
| 1 | MysqlConn.java | 工具 | JDBC连接+执行SQL |
| 2 | Fpage.java | 工具 | 分页计算 |
| 3 | ExcelBook.java | 工具 | Excel读写 |
| 4 | lwmAdmin.java | 实体 | 管理员 |
| 5 | lwmStudent.java | 实体 | 学生 |
| 6 | lwmTeacher.java | 实体 | 教师 |
| 7 | lwmSubject.java | 实体 | 科目 |
| 8 | lwmExamQuestion.java | 实体 | 试题 |
| 9 | lwmExamPaper.java | 实体 | 试卷(24字段) |
| 10 | lwmExamRecord.java | 实体 | 考试记录(状态机) |
| 11 | lwmStudentAnswer.java | 实体 | 学生答卷 |
| 12 | lwmExamScore.java | 实体 | 考试成绩 |
| 13 | lwmKnowledgePoint.java | 实体 | 知识点 |
| 14 | lwmQuestionKnowledge.java | 实体 | 试题-知识点关联 |
| 15 | lwmMistakeBook.java | 实体 | 错题本 |
| 16 | lwmstudentcourseteacher.java | 实体 | 排课 |
| 17 | lwmstudentDAO.java | DAO | 学生CRUD |
| 18 | lwmTeacherDAO.java | DAO | 教师CRUD |
| 19 | lwmsubjectDAO.java | DAO | 课程CRUD |
| 20 | lwmCourseArrangeDAO.java | DAO | 排课CRUD+排重 |
| 21 | lwmquestionDAO.java | DAO | 试题CRUD+随机抽取+重复检查 |
| 22 | lwmpaperDAO.java | DAO | 试卷CRUD+安全检查 |
| 23 | lwmscoreDAO.java | DAO | 成绩CRUD |
| 24 | lwmKnowledgePointDAO.java | DAO | 知识点CRUD+关联管理 |
| 25 | lwmMistakeBookDAO.java | DAO | 错题记录+掌握率分析 |
| 26 | lwmLogin.java | Servlet | 多角色登录验证 |
| 27 | SystemExit.java | Servlet | 注销 |
| 28 | lwmAddstudent.java | Servlet | 添加学生 |
| 29 | lwmquerystudent.java | Servlet | 搜索学生 |
| 30 | lwmstudent_xx.java | Servlet | 学生分页列表 |
| 31 | lwmUpdatestudent.java | Servlet | 修改学生 |
| 32 | lwmstudentDelete.java | Servlet | 删除学生 |
| 33 | lwmstudentExcel.java | Servlet | 学生Excel导入导出 |
| 34-39 | 教师CRUD(6个) | Servlet | 同学生结构 |
| 40-45 | 课程CRUD(6个) | Servlet | 同学生结构 |
| 46-51 | 排课CRUD(6个) | Servlet | 含排重检查 |
| 52 | lwmQueryQuestion.java | Servlet | 题库列表(按教师科目过滤) |
| 53 | lwmAddQuestion.java | Servlet | 添加试题 |
| 54 | lwmUpdateQuestion.java | Servlet | 修改试题 |
| 55 | lwmDeleteQuestion.java | Servlet | 删除试题(引用检查) |
| 56 | lwmLoadQuestions.java | Servlet | AJAX加载可选试题 |
| 57 | lwmCreatePaper.java | Servlet | 创建试卷(手动/自动) |
| 58 | lwmQueryPaper.java | Servlet | 试卷列表 |
| 59 | lwmUpdatePaper.java | Servlet | 修改试卷(提交保护) |
| 60 | lwmDeletePaper.java | Servlet | 删除试卷(提交保护) |
| 61 | lwmPublishPaper.java | Servlet | 发布管理(排课+提交保护) |
| 62 | lwmViewPaper.java | Servlet | 试卷预览 |
| 63 | lwmRandomPickQuestions.java | Servlet | AJAX随机抽题 |
| 64 | lwmManageKnowledgePoint.java | Servlet | 知识点CRUD JSON API |
| 65 | lwmQueryTeacherCourses.java | Servlet | 教师排课列表 |
| 66 | lwmViewClassStudents.java | Servlet | 班级学生名单 |
| 67 | lwmQueryExamRecords.java | Servlet | 考试记录列表 |
| 68 | lwmGradeExam.java | Servlet | 阅卷入口(自动评分) |
| 69 | lwmSubmitScore.java | Servlet | 保存评分(错题记录) |
| 70 | lwmDeleteExamRecord.java | Servlet | 删除考试记录 |
| 71 | lwmScoreAnalysisAction.java | Servlet | 成绩分析(动态及格线) |
| 72 | lwmQuestionQualityAction.java | Servlet | 试题质量JSON API |
| 73 | lwmKnowledgeAnalysisAction.java | Servlet | 知识点分析JSON API |
| 74 | lwmTakeExam.java | Servlet | 考试入口(时间+班级校验) |
| 75 | lwmSaveExamDraft.java | Servlet | 保存草稿 |
| 76 | lwmSubmitExam.java | Servlet | 提交考试(超时检查) |
| 77 | lwmViewExam.java | Servlet | 查看已提交试卷 |
| 78 | lwmUpdateStudentAnswer.java | Servlet | 修改已提交答案 |
| 79 | lwmMistakeBookAction.java | Servlet | 错题本+复习状态 |
| 80 | lwmKnowledgeMasteryAction.java | Servlet | 知识点掌握率JSON API |
| 81 | HelloServlet.java | Servlet | 测试 |
| 82 | login.jsp | JSP | 登录页 |
| 83 | lwmfoot.jsp | JSP | 分页组件 |
| 84-100 | 管理员端(16个JSP) | JSP | 框架+CRUD列表+表单 |
| 101-118 | 教师端(18个JSP) | JSP | 框架+题库+试卷+阅卷+分析 |
| 119-127 | 学生端(9个JSP) | JSP | 框架+考试+错题本 |
| 128 | web.xml | 配置 | 部署描述符 |
| 129 | lwmexam.sql | 配置 | 建表脚本 |
